# tmux-worktree 架构

Relay v2 Broker 的 client/Host WSS runtime composition 前现有两个独立、default-off 的 pre-101 Upgrade dispatch owner，共用一个只捕获 closed HTTP metadata、拒绝 Proxy/accessor 的 boundary。client owner 对合法 role=client v2 结果生成不可见 connectionId，并只把可信 auth snapshot 交给 current-Host prepare seam；Core 选择已验证 client identity 对应的 current Host carrier，唯一 producer registry 再解析 exact target，调用方不能注入 target；Host owner只接受 exact `/host`、空 query、唯一 `Authorization: Bearer twcap2...` 与唯一 `tw-relay.host.v2`，只以 expectedRole=host 调用 v2 verifier，再把 closed auth snapshot交给现有 `prepareHostWss`。两者都把 dispatch/Core preflight 的 400/401/403/404/426/503 reject 原样停在 HTTP 101 前；成功公开结果只有各自固定 subprotocol 与既有 opaque admission receipt，不暴露 token、auth context、producer target、connectionId 或 Broker Core。Host 侧另有独立、default-off 的 native Upgrade/brand owner：它先经 canonical Host WSS runtime 的 owner-bound one-shot claim 原子验证并消费 receipt，再调用 injected `noServer.handleUpgrade` 等价 port；native失败后原receipt也不能绕过该handoff direct attach或被另一Upgrade authority复用。随后只有该 exact callback 的同一、精确协商 `tw-relay.host.v2` 的 socket才会进入私有 `WeakSet` 并获得只读brand，再由claim把同一socket交给既有Host WSS attach；early/multiple/late callback、foreign receiver/socket/protocol、receipt replay、Upgrade/attach失败都会关闭对应transport并保持通用错误，close barrier会先fence新handoff再等待已接attach drain。该owner只公开trusted prototype、只读brand verifier与frozen handoff，不公开brand issuer/集合，也不创建WebSocketServer、listener、端口或网络。相邻另有一个独立、default-off 的 listener-free Node `ws` noServer adapter，以固定 Host subprotocol、关闭 compression/client tracking 和 canonical frame limit实现该 injected native Upgrade port，只关闭自己的 noServer WSS、不额外拥有或重复实现raw-socket cleanup；`ws` native handshake可能写错误或销毁socket，最终失败收敛仍由B7h authority负责，accepted connection也不由该adapter关闭或等待。其上的独立、default-off Host listener-free composition 现按固定顺序在同一闭包内创建 noServer adapter、native Upgrade/brand authority、canonical shared producer runtime 与 Host pre-101 dispatch；B7g receipt 只在同一次 `upgrade` 内立即交给 B7h claim，brand/prototype/native port、prepare/claim/attach、runtime/Core/registry 均不越过该深模块的 frozen null-prototype `hostUpgrade.upgrade`/`closeAndDrain` interface，成功 facade 也只返回 frozen `upgraded` outcome 与 connection，不公开 preflight selected protocol。它保留原 raw-verifier factory，并另有只接受 credential opener 与 bounded shared-runtime options 的独立 activation factory；activation 以 B7h 的 exact prototype/brand 激活现有 shared producer，把 opener 返回的同一个 exact credential authority 同时用于 Core auth-control/live fence 与 Host Upgrade token authorization，不接受第二 verifier、raw runtime、brand/prototype或credential/storage shortcut。authorizer 必须是该 exact authority 上可闭合捕获的 data method；missing/accessor/Proxy、authority未ready、live fence已关闭或后续装配失败都在 facade 发布前关闭 activated shared runtime及其 authority，再清理 B7h/B7i。composition close先同步 fence 新 Upgrade并等待当前 dispatch/native operation，再依次排空 B7h、shared runtime/credential authority 与 B7i；close race 在 native 前收敛为 503，accepted connection 仍由 B7h 先关闭并等待 runtime drain。该 composition 外现有独立、default-off、listener-free 的 Node `IncomingMessage` Host Upgrade request/reject adapter：它只从同一 request 的 raw target 和 `rawHeaders` 捕获 frozen B7g metadata，Authorization 重复项保持原顺序，`Sec-WebSocket-Protocol` 重复项按逗号拆分并只裁 HTTP OWS，legacy query secret 固定为 null；无效 target/header shape 在调用 B7j 前固定以无 body/no-store/connection-close 的 HTTP 400 拒绝，B7j 的 400/401/403/404/426/503 也只映射为固定最小 pre-101 response。B7j 一旦返回 upgraded 或抛错，该 adapter 不再 write/end/destroy raw socket；close 会同步 fence 新 request、立即并行关闭 B7j 并等待当前 handler，公开面只保留 frozen null-prototype `handleUpgradeRequest`/`closeAndDrain`，不暴露 metadata、reject detail或connection handle。相邻的 credential-activated Node listener-free Host ingress 只把原 options 交给 B7l activation，成功后唯一构造并直接返回 B7k facade；B7l activation失败仍由其自身回滚，B7k构造失败则由外层 exactly-once 排空已发布 B7l，正常关闭完全复用 B7k 的同一 Promise 与顺序，并且不暴露任何内层 owner。其上另有独立、default-off、未接线的 external-continuity-backed Host ingress activation plan：构造期只以 closed 顶层 capture 创建现有 inert E0 opener，不调用 native loader/provider/store、HTTPS/TLS、listener 或 network；唯一 one-shot `activate()` 才把该 opener 与 captured shared-runtime options 作为 exact B7m 输入，并直接把唯一 B7k ingress ownership 交给 caller。失败完全复用 E0/B7m rollback，plan 不增加 close、coordinator、retry 或 fallback；它没有 production callsite，production v2 继续 disabled。相邻的 default-off、listener-free one-shot activation 先以 dormant auth-control bridge 构造同一 registry/output-ready/Core/client owner，只把该 Core 的 exact live-authorization fence 交给 injected credential opener，并在 authority ready 且原子绑定后才发布 runtime；shared producer activation 复用该 exact registry/runtime 与同一 managed Pump set，总 close owner 依次收敛 Pump、client runtime、auth-control task 与 credential authority。Host pre-101 dispatch owner本身不读取 socket、不签 socket brand；上述 owners 现由显式、default-off 的 activated Broker server composition 在同一闭包内采用，并可作为可选参数交给 `startRelayBroker`；相邻的显式 default-off public lifecycle root 则接管 caller-supplied、otherwise-unowned `node:https.Server`，激活该 composition，并唯一拥有 request/upgrade/listen 与 close-and-drain。CLI `run()`仍不调用该 root，composition不创建capability/enrollment/retry/fallback。未注入 base capability readiness 时 client admission 仍为 426，`qualifiedRecords=[]`仍使真实native authority在activation前fail closed，且无qualified E0、真实public TLS/device interoperability/signing/release证据；production client/Host Upgrade继续NO-GO，默认shipping Relay路径仍是v1。该 Broker Host WSS runtime composition 现另在 101 后 arm 一次性 5 秒 host.hello deadline（default-off、可注入 handshake scheduler）：只有 Core enqueue `host.registered` 才取消该 deadline，到期时仍以 open 未注册状态经 owner `beginClose(4408, "handshake_timeout")` 关闭；scheduler install/cancel 失败 fail closed 为 1013 `host_handshake_timer_failure`，deadline-object identity 阻止 stale callback 关闭 replacement，`rollbackConstruction`/`beginClose`/`observeTerminal` 均取消 deadline。它不新增 retry、dialect fallback、capability advertisement 或 enrollment 行为。

client 侧相邻现有独立、default-off 的 Node listener-free WSS ingress，只接受外部 HTTP(S) server 显式交付的同一 `IncomingMessage`、raw socket 与 head；producer target 只由 Core-selected current Host carrier 与唯一 registry 解析，调用方没有 target 字段。构造时它从 canonical client runtime facade 的私有 WeakMap 消费独立 one-shot ingress claim，并把本 entry 的 exact `WebSocket.prototype` 与只对本次 native callback socket 成立的私有 brand 固定给 client adapter；adapter 的方法和事实 descriptor 只从该 trusted prototype 捕获，不信任实例或任意 prototype 上可伪造的结构性 accessor。client claim 与已有 Host claim 各自一次、互不抢占。它从 raw target/`rawHeaders` 单次执行既有 client pre-101 dispatch，严格要求 `/client`、空 query、唯一 `tw-relay.v2`，再以私有 `WebSocketServer({ noServer: true })` 禁用 per-message deflate/client tracking 并限制 canonical frame size；101 后只把同一已升级 socket和该次 opaque receipt 一次性交给既有 runtime attach。101 后 attach/close 竞态会先 terminate socket；私有 child 只排空 noServer 与在途 handler，canonical runtime total close 只由 shared owner 持有，且绝不重跑 Phase A。公开面只有 `handleUpgradeRequest` 与 `closeAndDrain`；模块本身不 listen/bind/create HTTP(S) server；它只被显式 default-off activated Broker server composition采用并复用外部server，默认CLI不注入该composition，也不生成enrollment、ready/capability、retry或v1 fallback。

该 Node request/reject adapter 还要求 `IncomingMessage` 自身的 socket data descriptor 与调用方 raw socket exact 同一，并将 `rawHeaders` 闭合为 length 与全部索引组成的 own-data key 集；pre-101 graceful end 受模块内部固定 1000ms deadline 约束，caller 不能配置或替换，callback 不返回或 end 失败时 exactly-once destroy 并通用失败。handler 在异步 body 启动前登记独立 drain barrier，因此同步重入 close 也会等待 reject flush；这些约束不改变 upgraded/possible-101 socket 仍完全归 B7h cleanup 的边界。

本文描述当前源码已经实现的边界。用户安装与开发命令见 `README.md`，Agent 的阅读顺序和变更门禁见 `AGENTS.md`。如果文档、contract、源码和测试冲突，先以当前可观察生产行为与版本化 contract 确认兼容承诺；源码和测试都是证据，不能仅凭某个内部结构测试反推架构。确认后在同一变更同步修正冲突项。

## 架构结论

仓库有三类可独立发布的产物：Node.js CLI、macOS Dashboard 和 Android APK。三端生产源码彼此分离，但共享协议和运行时约束：

- 根目录 `tw` CLI 是 managed worktree/session 的 headless control plane，也是 Relay broker/connector 和 Web terminal 的 Node.js runtime。
- macOS Dashboard 是 React UI + 模块化 Rust/Tauri 后端。它不维护第二套 managed session creator，而是通过同版本 `tw rpc` 执行创建、恢复和关闭。
- Android 是独立 Gradle 工程和原生 Kotlin/Compose 客户端。它通过 Relay v1 使用 Mac connector 暴露的能力，不链接 React、Rust 或 Node.js 生产代码。
- Relay v1仍是默认shipping移动协议；Android另有只面向已admit显式profile的受限v2 base runtime。`docs/relay-v2-contract.md` 与 `contracts/relay/v2/external-continuity-authority-v1` 仍是frozen contract；Node已有仅供default-off composition注入的external HTTPS adapter 也不代表已有 production backend/auth/config/injection、端到端shipping已经支持v2，或可以进入 ready/capability advertisement。
- 独立的本地 terminal-control v1 是 Dashboard、受控 CLI、Relay v1 和 Feishu Bridge 共同使用的 terminal input authority；Feishu 与 Relay 不共享 transport、鉴权、credential 或业务协议。

最重要的所有权规则是：`tmux` 保存活进程事实，`tw` 保存 managed lifecycle 事实，local terminal-control 保存 Feishu/interactive 两类 input ownership、共享 lease/fence 和 single-writer 顺序，Feishu Bridge 保存 binding/turn/reply 事实，Dashboard 保存桌面展示状态，Android 保存手机侧缓存与待发送状态。Dashboard、APK/Relay、受控 CLI 和 `tw serve` 属于同一个 interactive 类，彼此不争锁；只有 Feishu 会独占一个 terminal。展示层缓存不能决定一次写入或破坏性操作应走哪条权威路径。

## 顶层依赖方向与变更落点

仓库以“权威状态与写路径”组织代码，不以页面、需求阶段或发布版本复制垂直功能。稳定依赖方向如下：

```text
CLI command / RPC / Relay adapter ──> tw domain owner ──> git / tmux / state

React view ──> renderer model/hooks ──> DashboardBackend
                                      └─> Tauri adapter ──> tw RPC or native OS capability

Compose screen ──> V2ViewModel ──> repository / Relay actor ──> storage / codec / socket

cross-surface implementation ──> versioned RPC / wire / storage contract
```

维护代码时遵守以下落点规则：

1. 状态、写权限和生命周期判断落在已有 authority owner；UI、transport、IPC DTO 和 composition root 只能传递意图、适配数据或编排调用。
2. 上层依赖下层公开边界，下层不反向依赖具体页面或 transport。跨发布面不直接共享实现；需要互操作时先定义或沿用版本化 contract。
3. 新模块只在形成独立职责、状态或生命周期边界时成立。单调用 wrapper、为测试导出的内部函数、按任务阶段拆分的文件，以及与现有 owner 并行的 `new`/`v2`/`legacy` 实现都不是架构分层。
4. 一次替换应收敛到一条生产路径；除明确支持的兼容入口外，不保留双写、双读或“失败后试另一套实现”。兼容入口必须枚举触发条件并有删除标准。
5. composition root 可以装配大量能力，但不能成为业务 owner。文件变长不是复制 owner 的理由；应先把逻辑移动到正确的现有 feature/domain，再判断是否需要新边界。
6. 架构不由精确文件布局、export manifest、函数调用图或组件树定义。只要 authority、依赖方向、contract 和可观察行为不变，内部结构应可自由重构；测试不得冻结这些实现形状。

代码审查首先检查 owner、依赖方向、canonical 写路径和失败语义，再检查局部实现。测试策略与新增准入见 `AGENTS.md`；测试用于保护行为和契约，不作为增加抽象或保留重复实现的理由。

## 发布物与源码边界

| 发布物 | 生产源码 | 构建结果 | 运行时边界 |
|---|---|---|---|
| `tw` / `tmux-worktree` CLI | 根目录 `src/` | `dist/cli.cjs`；根包还生成供测试和 import 的 ESM 模块 | 需要 Node.js 20+、git 和 tmux；同时包含 `serve`、`relay-server`、`relay-host` |
| macOS Dashboard | `app/src/` + `app/src-tauri/` | `tw-dashboard.app`、DMG；根包的 `tw-dashboard-install` 安装 DMG | `.app` 内含同版本 `dist/cli.cjs`，但不含 Node.js runtime |
| Android App | `mobile/android/` | Debug APK 或 Release APK | 独立 Kotlin/Compose 应用；不打包 CLI、React 或 Rust |

`contracts/` 是跨实现的机器可读契约，不是第四个运行时产物。Android 的 Gradle test source set 会读取 `contracts/relay/v1` fixture；这些 fixture 不进入 APK 的生产资源。

## 代码地图

### 根目录 TypeScript：CLI 与 Relay runtime

| 路径 | 职责 |
|---|---|
| `src/cli.ts`, `src/commands.ts`, `src/dev.ts` | CLI 路由、交互命令和直接创建开发 session 的入口 |
| `src/session.ts`, `src/state.ts`, `src/rpc.ts` | single-pane managed session、`~/.tmux-worktree/state.json` 和 TW RPC v1 |
| `src/tmux.ts` | git/tmux 进程边界与底层查询 |
| `src/config.ts`, `src/hosts.ts` | 项目/Host 配置、兼容解析、SSH ControlMaster 和远端 RPC |
| `src/automation.ts` | Dashboard 共用的本地 automation 定义 |
| `src/serve.ts` | 带 token 的本地 HTTP/WebSocket 终端后端 |
| `src/relayHost.ts` | Mac admin connector；聚合 local 与显式配置的 SSH scope |
| `src/relayServer.ts`, `src/relay/broker/` | 默认 CLI 的 Relay v1 鉴权、Host/Client 路由和连接生命周期；`startRelayBroker` 另接受显式 default-off v2 activated composition |
| `src/relay/v1/` | Relay v1 wire model 与消息类型 |
| `src/relay/v2/` | Relay v2 独立 strict codec、Broker/Host carrier 与 ingress、H0–H3、credential、materialized-state/snapshot、terminal、canonical runtime bundle，以及显式 default-off Broker/Host composition 与 shipping owners。Host composition 只消费 canonical H1/H2 authority cuts并拥有唯一 route/readiness、managed connector、credential-exact WSS 与 production welcome owner；`relay-host --profile v2` 现只经唯一 trusted deployment activation/source 单次冻结 exact profile，把同一 Host lineage、revision-7 trusted native source、canonical runtime bundle及两条独立 TLS trust cut 交给 shipping root。默认 CLI/shipping 仍为 Relay v1；Host `qualifiedRecords=[]`，E0/qualification/recovery与真实 TLS/process/network/device 证据仍缺失，显式 v2 在 socket/capability advertisement 前 fail closed，无 v1 fallback，也不构成 production readiness。native foundations 位于下列独立 crates。 |
| `src/relay/extensions/agentTranscriptLifecycle/v1/` | 已有canonical Host可选attachment、但无默认production activation的Agent transcript/lifecycle authority、public codec、durable store/replay runtime、Codex `0.144.5` schema 2 producer、bounded LF notification source、injectable/default-off process-controller lease authority、trusted-source composition与one-shot activation foundations。activation只接受controller、既有runtime与canonical resolver，私有构造issuer/receiver/authority/composition并固定执行controller/source claim→opaque lease→独立H2 evidence校验→attach；联合close在handoff前后分别由authority/composition唯一drain同一raw source，并等待producer/ingress FIFO。built activation entry复用direct controller/source entry的进程registry，关闭或失败后不可重领。controller lease不能替代H2；这些模块不启动、发现或认证真实Codex进程，也不是production app-server ingress、external continuity backend、route或capability wiring |
| `contracts/relay/v2/external-continuity-authority-v1/` | frozen future external authority互操作/安全contract与compact machine fixture；定义rollback-independent linearizable read/CAS、provisioning/ACL、closed internal error mapping、恢复、namespace lifecycle和ready-loss fence准入，不包含backend/transport adapter、secret material或production wiring |
| `native/relay-v2-broker-credential-state-store-core/` | 独立未接线的纯 Rust broker credential binary/publication core；拥有 bounded absolute-range selector、owned FIFO admission/transaction lease、opaque revision、两阶段 publication/uncertain poison/close barrier，不属于根 Cargo workspace，也不实现 N-API、path/JSON、OS secure-open/identity/kernel-lock/真实 durability 或业务 schema |
| `native/relay-v2-broker-credential-state-store-platform-common/` | 独立未接线的 Darwin/Linux shared lifecycle owner；build script单点消费canonical private-location manifest，runtime拥有显式eager process token、pre-fd registry、PID/descriptor fence、panic-aware poison、common-owned exactly-once final close、private N1 bridge与opaque wrappers。它不构造path、不实现platform syscall/filesystem/durability或N-API；N2/N3/N-API不得直接消费N1或复制lifecycle |
| `native/relay-v2-broker-credential-state-store-platform-darwin/`, `native/relay-v2-broker-credential-state-store-platform-linux/` | 独立未接线的 target platform adapter foundations；只经 platform-common public lifecycle seam 实现各平台 secure-open/lock/durability primitive。冻结 qualification allowlist 为空，因此 production real open 在 registry/mutation 前 fail closed；cfg(test) qualified scaffold 不是 platform-adapter-qualified real-open 证据 |
| `native/relay-v2-broker-credential-state-store-napi/` | 独立未接线的 raw N-API binding foundation；module init eager exactly-once 捕获 process lifecycle token，只把 platform-common `ProcessBound*` 与 target platform open seam 转成冻结 ABI。另有显式、未接线的本机 target build/stage/npm-pack verification foundation，共用 loader 的四项 fixed descriptor并验证binary header、固定命名、tar/unpacked layout与closed binding；当前只有 Darwin arm64 证据，没有 qualified real open、actual opened JS transaction、production packaging/wiring 或其他 target 证据 |
| `native/relay-v2-host-credential-atomic-file-cell-platform-common/`, `native/relay-v2-host-credential-atomic-file-cell-platform-darwin/`, `native/relay-v2-host-credential-atomic-file-cell-platform-linux/` | Host credential cell 专属、独立未接线的 platform-common admission/mutation owner与Darwin/Linux adapter；common只消费预绑定 directory descriptor 和 injected descriptor-relative trait，独占 Host directory `(dev, ino)` process registry、opener-PID/fork fence、traditional nonblocking whole-file `F_SETLK` lock、fixed binary claim journal 与 exactly-once close/tombstone。common 在接管 directory descriptor 并通过 parent-PID fence 后、任何 registry/lock/claim/fsync/namespace mutation 前，以 OS CSPRNG 一次签发 exact 32-byte non-zero opaque claimId；entropy error或全零固定为`CELL_IO`并只raw-close已接管directory一次，不接受caller claimId，也不fallback/predict/retry。顶层 contract revision 7 保持credential mutation/native ABI/fixture/platform resource/claim journal各为v1，并 additive 冻结 trusted factory v1（见相邻段落）；common在同一`AdmissionOwner`内通过additive mutation子trait实现read、opaque revision、双current-check CAS、tracked temp、rename publication、cleanup与terminal fence，Darwin 与 Linux crate也已分别实现该mutation trait的descriptor-relative syscall seam，既有admission trait完全不变。两个target的独立真实验证分别覆盖 `aarch64-apple-darwin` 与 `x86_64-unknown-linux-gnu` 的credential read/temp/write/fsync/unlink/rename filesystem操作，以及subprocess `F_SETLK` busy/close-release与exec `FD_CLOEXEC`；仍没有Darwin x86_64或Linux arm64证据。三者不依赖或复用broker N0 namespace/path/format/registry/owner，也不实现完整admission、durability qualification、recovery/enumeration、Vault/Authority或`relay-host` production composition；revision 7 起两个 adapter 另各自实现 trusted factory 的 native directory producer（credential snapshot proof、getpwuid account-home proof、contract 固定 private-location no-follow secure open，只产出预绑定 directory descriptor，不创建/修复/选 alternate），同样 default-off 未接线；raw N-API binding 与 fixed loader/build-stage-pack 由相邻独立 crate 与 script foundation 提供（见下一行），同样 default-off 未接线；claim journal仍只证明admission。`qualifiedRecords=[]`且所有production/admission/durability/capability边界仍为false/none，所以完整admission adopt与production mutation路径仍不可达。纯`credential-cell-read-cas-temp-or-rename`实现缺项已从全局`notImplemented`移除；这不表示power-loss、production wiring、readiness或capability |
| `native/relay-v2-host-credential-atomic-file-cell-napi/` | 独立未接线的 raw N-API binding foundation；module init eager exactly-once 捕获 platform-common process lifecycle token 与 process-origin pin，exports 保持 frozen v1 exact 单 `openRelayV2HostCredentialAtomicFileCellV1` 表面，closed 校验请求后经空 qualification allowlist 在 registry/mutation 前返回 `CELL_DURABILITY_UNSUPPORTED`，raw open 仍不触碰 filesystem/env/HOME。Revision 7 起 raw open function 另携带 additive own-data trusted factory entry `createRelayV2HostCredentialAtomicFileCellTrustedFactoryV1`（不声称可见性隔离），其唯一 production driver 是 fixed trusted loader；factory 经 target cfg 依赖对应 Darwin/Linux adapter 的 native producer，由 getpwuid account-home proof 与 contract 固定 private-location no-follow secure open 取得 exact owner/0700 directory descriptor，mint 不可伪造 capability，one-shot binder 只交付 exact own-data `{open}` 的最终 v1 module，最终 module 与 raw open 都不接受 path/fd/HOME/env/credential。最终 module 的 qualified-success 分支现已 one-shot take exact capability，把 platform+descriptor 移交唯一 platform-common `AdmissionOwner`，并把同一 owner 投影成 frozen v1 exact own-data `read`/`compareAndSwap`/`close` handle；opaque revision 仍由 common generation/fence定义，N-API 只发布 owner-bound one-shot transport value，未增加第二 mutation/credential owner。空 `qualifiedRecords=[]` 下该分支仍不可达，最终 module 与 raw open 都在既有 durability gate 前失败，不宣称任何 target 的 qualified real open/readiness/capability。另有显式、未接线的本机 target build/stage/npm-pack verification foundation（`scripts/relay-v2-host-credential-native.mjs`），复用 Broker 的 fd-owned stage 与 bounded ustar inspector 安全边界，验证 Host 四项 fixed descriptor 的 binary header、固定命名、tar/unpacked layout 与 closed binding，并按 owner 精确校验本 owner artifact、只允许同 target 冻结 Broker sibling artifact 共存、拒绝未知/重复/错 target 与非 regular file；当前只有 Darwin arm64 closed-gate 证据，没有 qualified opened-handle runtime evidence、production packaging/wiring 或其他三 target 证据 |
| `src/terminalControl/` | 本地 input ownership contract client/server、lease/fence authority、受控 backend write 与 output cursor |
| `src/feishuBridge*.ts`, `src/feishuReplyCard.ts`, `src/larkCliBridge.ts` | 独立 Feishu binding/event/turn/reply daemon、Card JSON 2.0 builder 与 Lark adapter；不调用 Relay transport |

`tw rpc` 是给 Dashboard、Relay 和 headless agent 使用的机器接口。当前协议提供 capabilities、list、create-worktree、create-terminal、restore-worktree 和 kill-session；JSON shape 由 `contracts/tw-rpc/v1` 固化。

### macOS 前端：React Dashboard

`app/src/App.tsx` 是组合根，不直接调用 shell、tmux 或 SSH。所有外部能力先经过 `app/src/platform/DashboardBackend`：

```text
React view / hook / coordinator
          |
          v
DashboardBackend interface
          |
          +-- tauriBackend   -> production Tauri invoke/event
          +-- fakeBackend    -> unit tests
          `-- previewBackend -> browser/design preview
```

主要边界：

- `app/src/dashboard/`：Dashboard shell、sidebar、workspace、settings、hooks、布局和纯模型。
- `app/src/platform/`：领域类型、Backend facade、Tauri transport 和测试/预览实现。
- `Terminal.tsx`、`FileTree.tsx`、`FileEditor.tsx`、`GitStatusPanel.tsx` 等：功能视图；通过 Backend 获取数据。
- `NewWorktreeModal.tsx`、`NewTerminalModal.tsx`：收集用户输入，不拥有后端生命周期规则。
- `terminalPersistence.ts`：桌面 terminal metadata 的恢复与兼容规则，不是 tmux runtime registry。

React 持有选择、打开面板、草稿和轮询状态；它不持有 managed session 的权威状态。

Dashboard 已有一个通过 `DashboardBackend` 驱动的 Relay v2 enrollment/process 领域切片，用于区分 Relay v1 shared-secret profile 与 v2 host credential reference，并建模 host bootstrap/refresh、`host.registered`、六项基础 capability、一次性 enrollment、已知 client grant revoke、过期和 SUPERSEDED。浏览器 preview 使用明确标记的内存 fake。Dashboard management IPC v2 consumer foundation 已闭合 Node authority → Rust child supervisor → Tauri command → TypeScript adapter → 现有领域模型的非敏感投影：Rust parent固定钉扎V2，独占`dmgmt2.*` requestId、单in-flight、deadline、framing与child lifecycle，任何ready mismatch都kill/reap且不respawn、不切版、不fallback。Tauri只接受closed operation/input；bootstrap/access/refresh/v1 shared secret不进入IPC；一次性enrollment code只到Tauri strict decoder和有界内存QR registry，renderer只收到可撤销opaque handle及非敏感facts。Node protocol-v2 authority只消费same-lineage credential adapter、Host-owned connector cut与registered HostCarrier control，不复制credential/controller/carrier owner；start/stop generation、registered cut、enrollment/revoke correlation和drain仍由原owner决定。protocol-v2 composition session通过canonical Host managed port的exclusive claim绑定同一串行stdio owner，公开面只有one-shot run与close-and-drain。Tauri bundled hidden child现是它的显式default-off CLI activation：child只把stdio/runtimeVersion/clock/signal交给唯一trusted Host opener，由同一个root的`dashboardManagement` seam装配same-lineage composition；没有raw authority注入、第二owner、auto-retry或v1 fallback。当前Host `qualifiedRecords=[]`仍让open在native mutation/socket前失败并返回typed `UNAVAILABLE`，所以Dashboard仍不能生成真实enrollment或宣告ready，默认生产Relay runtime仍只有v1。

Dashboard `.app` 的 `tw-cli/` resource现同时携带standalone `cli.cjs`、提供`type: module`边界的root `package.json`与完整canonical dist sibling tree。hidden child仍从CJS入口启动，但trusted source/root及Host composition按原ESM sibling identity加载；它们不被重打进CJS，也不会因`.app`缺文件而落回selection-only `UNAVAILABLE`。

protocol-v2 composition session 通过 canonical composition owner 的 fresh exclusive claim 激活：任何既存或已关闭 composition activation 都不能转为 session，session claim 也不能被普通 composition factory 重取 request handle。调用方 AbortSignal 在该 claim 前通过捕获的原生 descriptor 完成 brand、canonical prototype 与 own override 校验，只保留为 exact session identity 与外部 abort source；abort 被转发到 session-owned AbortController，composition、credential 和 connector 只接收内部 signal，后续监听和清理不再通过调用方属性查找。

Controller 与 canonical HostCarrier 之间另有独立、未接线的 attempt adapter。每个 controller generation 只调用一次注入 factory；factory 必须在 actor 构造时注入 adapter 提供的 exact status callback，并返回该 actor、exact transport 与 opaque drain handle。adapter 完整捕获并验证三者后才以 exact credential reference 调用 actor `connect()`；只转发与 actor 当前 canonical status 相同的回调，connecting 不会被提升为 registered。`disposeAndDrain` 先关闭 callback fence，再 exactly-once 调用 actor dispose 与 transport close，并只在 injected native Promise 返回同一个 opaque proof 后回传 controller 的 exact generation/carrier generation/connectorId evidence。factory、attempt、callback fence 与 drain authority 不通过对象属性暴露；malformed、foreign proof、factory throw 或 drain uncertainty 统一 fail closed 且不反射敏感错误。该 adapter 不拥有 controller 或 carrier 状态机，不创建 WSS、process、credential、retry timer、readiness、capability 或 v1 fallback，也没有 production composition callsite。

同一 default-off Host foundation 现另有 closed managed connector/runtime composition。它复用 direct carrier composition 的唯一 route/readiness/binding bridge，在闭包内构造 exact controller、attempt adapter 和每个 canonical HostCarrier actor；外部 factory 只能返回 transport、`bindConnection` 与 native-Promise drain evidence，不能注入 actor 或第二个 status subscriber。每个 actor exact 绑定同一 hostId/hostEpoch/hostInstanceId、唯一 route sink、空 advertised capabilities、`tw-relay.v2` dialect 和无 adapter 的 dialect set。controller 独占 start/stop/generation/fence，actor 独占 carrier state/queue/route，Host runtime 独占 route/readiness；composition 只装配并按“永久 fence 新 start → controller stopAndDrain → runtime dispose”关闭。公开 handle 以普通 frozen own property签发一个opaque、one-shot Dashboard management port；issuer closure要求helper把candidate callable本身作为显式首参并与exact port/binding比较，因此普通copy、bound callable copy与Proxy均跨dist fail closed，不依赖module-global registry。port exact绑定签发时的controller generation、同一Host/credential-reference identity与私有的exact credential owner identity，只有protocol-v2 session可领取为closed binding。领取只建立暂时fence公开mutation的pending reservation，consume也只进入pending；完整composition构造后exact commit才永久fence公开lifecycle并授权closed binding。构造失败会永久作废旧binding；只在generation未推进、composition未close且没有lifecycle/carrier side effect时exact abort并清除reservation，使原公开start/stop/reauth与原port可重新领取。foreign identity/credential owner、replay以及领取前已经start/stop推进的旧generation均在触碰credential、transport或输出前拒绝；commit后的closed binding只可通过自己的exact start/stop推进authorized generation，并在stop/replacement/close后不再取得旧actor control。handle其余公开面仍为inspect、start、stopAndDrain、exact-current requestReauthentication、readiness、sendTerminalFrame与closeAndDrain，其中reauthentication只用闭包内canonical actor与固定credential reference委托既有credential authority，registered因固定空intersection只投影为`registered_incomplete`，不暴露actor、controller、transport、token、credential owner、factory、sender或route owner。pending factory、offline后显式retry、replacement/late callback、route unbind与并发close均经原owner收敛；该composition不创建WSS、child/process、credential、timer retry或fallback；host.hello 的 capability advertisement 由 canonical runtime 的 readiness → one-shot pre-carrier claim → connect consume 链控制，actor 构造时的静态空 advertised capabilities 只是禁止旁路的 fail-closed 输入。`relayHost.ts` 的唯一 default-off canonical production opener 在 managed owner 构造完成后依次等待 H0、H3 activation 成功，才发布 facade；任一 false/throw 都沿既有 rollback 收敛且不启动 connector。显式 Dashboard management 配置由同一闭包唯一领取上述 same-lineage port并拥有 protocol-v2 session，省略时 facade 形状不变。CLI `run()` 与 public production activation 仍未调用该入口。

上段最后一句只适用于默认/no-profile 的 Relay v1 选路。显式 `relay-host --profile v2` 现经唯一 trusted deployment source 打开同一 canonical root；source 先复用 terminal-control client 的 auto-start 与现有 daemon lock/single-writer lifecycle，确认 canonical daemon 和 exact sibling ingress ready，再交给 runtime bundle preflight。root 发布的同一个 shipping handle 由独立进程生命周期 owner 持有并完成初始 start；retryable offline 只在 capped backoff 后通过同一 handle 重开 attempt，因此 hostInstanceId 不变；`SUPERSEDED` 永久终止。SIGINT/SIGTERM、普通失败与 superseded 都先按当前 exact generation/connector 调用 `stopAndDrain`，再 `closeAndDrain`。该外层 owner 不改变 composition 自身“不拥有 timer retry/daemon”的边界，也不改变空 capability、default-off、NO-GO 或无 v1 fallback。

同一显式 v2 CLI lane 现提供唯一 operator provisioning 入口 `--provision-profile-input <path>`。该 path 非 secret 且没有 env 等价入口；profile store 以 `O_RDONLY|O_NOFOLLOW` fd-bound 读取，验证 current uid owner、regular、single-link、exact `0600` 与 16 KiB 上限，再用现有 strict JSON decoder 和冻结 `host-production-profile-v1` closed schema拒绝缺字段、duplicate/unknown key、非 root `wss://`/`https://` URL、非法 reference 或任何 token-shaped secret material。CLI 只把所得 frozen reference-only snapshot交给现有唯一 safe writer `loadOrCreateRelayV2HostProductionProfile`；writer仍独占固定 `~/.tmux-worktree/relay-v2-host/profile-v1.json` 的原子 0600 create/no-replace语义，exact existing幂等，different/corrupt/unsafe/locked一律不覆盖。随后 trusted deployment source仍独立只读并冻结 canonical fixed profile，不接受 caller snapshot。该 option 可与首次 `--bootstrap-secret-input` 同次使用；后续启动省略两者并复用同一profile/Vault。v1拒绝该option，任一读取、contract、writer或后续activation失败都fail closed且不回退v1；raw bootstrap/credential secret不进入profile、argv/env/log/error/stdout。

首次 Host 可在同一显式 lane 增加 v2-only `--bootstrap-secret-input <path>`；path 非 secret，v1 lane 拒绝。唯一 trusted deployment source 复用现有 Node/libuv trusted-reader 模式，以 `O_RDONLY|O_NOFOLLOW` 单次打开该文件，再只对 fd 验证 current uid owner、regular、single-link、exact 0600 与既有 `maxRawBytes` 上限，并把该 fd 的唯一 Node Readable 包装成既有 `hostBootstrapSecretNodeReadableByteSource`，随同一 opaque activation 交给 shipping root。fd不暴露给业务或child stdio，其descriptor只由Readable/source cancel/close拥有。root 不再把该字段固定丢弃，而是继续进入既有 `hostBootstrapSecretSource` → handoff → Vault/credential authority；single LF framing由既有 source 直接消费，token 语义仍只属于 handoff/Vault。source/activation/root 的失败、abort与close共用原逆序 drain并关闭 fd，不自动删除输入文件、不建第二 credential owner、不回退 v1。bootstrap成功后的credential仍落入同一profile/Vault；后续无input启动及same-lineage Dashboard management均复用该owner。

仅本机 loopback 开发可显式选择 `--profile v2 --local-development`，且必须提供 current-user-owned、canonical、现存、exact `0700` 的绝对 `--trusted-home`，并分别提供 `--credential-https-ca-input` 与 `--carrier-wss-ca-input`。local deployment owner在任何 profile create、terminal-control 或 runtime open 前验证该 home；profile 的唯一 safe writer、读取、terminal-control socket/daemon 与 canonical runtime 都绑定它，production trusted source 仍固定 `homedir()`。该 activation 继续由同一个 Host deployment source 冻结 fixed profile、bootstrap byte source、canonical runtime bundle 与两条独立 TLS cut，并交给同一个 shipping root；root 只在 privileged intake 的最窄入口把 production native cell opener 换成单个进程内 atomic credential cell，H0/H2/reconcile、Vault/authority/HTTPS、carrier/controller/WSS 与 process lifecycle owner 均不复制。两个 profile endpoint 必须是 loopback；两个 CA 输入都以 no-follow fd 验证 current uid、regular、single-link、exact `0600` 与 bounded size，TLS 仍强制证书链和 hostname 校验。内存 cell 每个进程从空状态开始，按 canonical atomic-cell 语义不 assimilate 且拒绝 Promise、own/inherited/hostile thenable，并在 close 时清空，因此冷启动仍需既有 bootstrap 语义；它不持久化 credential。local source 还提供上层显式采用同一 shipping root optional Dashboard management session 的入口，session继续把 `start_connector`/`enrollment.create`送入既有 controller/carrier/credential authority且不采用 auto-start/retry process lifecycle，不产生第二 enrollment authority。该开发 lane 无 env 等价入口，不读取、修改或伪造 production `qualifiedRecords`，也不改变默认 v1、production `--profile v2` 的 native fail-closed、空 capability 或整体 NO-GO。

该 process signal owner 先于 trusted activation 安装，并把同一 signal 传给 daemon readiness、activation owner、runtime-bundle/root open 与后续 connector lifecycle。startup signal 先 fence 后续阶段；terminal-control request 可直接 abort，其他既有不可中断 await 在返回后观察 fence并走原 rollback。首轮 reconcile 期间 signal 还会调用唯一 reconcile lifecycle 的 `close()`，使其拒绝后续 timer/scan并等待当前 scan drain；若 privileged intake/root 已完成发布，则 owner 不在 activation 层复制 close 路径，而是把已 abort signal 交给同一个 shipping handle lifecycle。cleanup uncertainty 继续 fail closed。

相邻的单 profile Host credential vault 现有一个独立、default-off 的 process-local bootstrap-secret handoff authority。它以闭包分离 privileged intake 与 Vault-facing port，只接受有界 printable `twhostboot2.*`，并用空外形、one-shot candidate 在内存中完成一次同步交接；foreign、copy、Proxy、replay、并发 consume 与 close 后 candidate 都在 callback 前 fail closed，callback throw 只恢复尚未关闭的 candidate，正常返回才清除 secret reference并提交 consumed。close 会同步 fence 新 intake/consume、等待已进入 callback 返回并清除未消费 secret。Vault 继续独占 atomic byte cell/envelope/CAS，`RelayV2HostCredentialAuthority` 继续独占 credential state，HTTPS adapter 继续独占 exchange；handoff 不读取 stdin/file/env，不创建 storage、process、network、timer、connector、retry、fallback 或 capability。该handoff现由显式default-off privileged intake composition采用；outer owner可接受唯一 trusted activation 已读并冻结的 exact profile snapshot，构造同一Vault/credential authority/HTTPS coordinator并打开canonical Host composition。这个唯一 startup owner 会在 canonical open 前处理首次 source provision、durable bootstrap-only envelope 或恢复的 credential version `0`：读取 exact HostState snapshot 的 `hostEpoch` 与同一 `hostState.hostInstanceId`，使用外层同一个 startup signal 调用既有 coordinator；pending attempt 仍由 authority 原语复用，成功仍只由同一 authority/Vault commit，ready credential 不重复 bootstrap。exchange/commit/canonical 任一失败继续走同一逆序 rollback，Dashboard management 不是 bootstrap 的必需 caller。它现由显式 default-off Host shipping root（`src/relay/v2/hostShippingRoot.ts`）装配；唯一 Host trusted deployment source（`src/relay/v2/hostShippingDeploymentSource.ts`）把同一 profile/Host lineage、revision-7 trusted native source、canonical runtime bundle 与两条独立 TLS trust cut 封装为 opaque one-shot activation。`relay-host --profile v2`的headless process lifecycle和Dashboard hidden child的management lifecycle是该source的两个显式进程入口；每个进程只持有一个root/handle，Dashboard入口不auto-start/retry connector。source 在 bundle preflight 前只通过现有 terminal-control client/daemon lifecycle 准备 canonical daemon 与 exact sibling ingress，不复制 daemon authority。任一失败逆序 drain、fail closed 且不回退 v1；`qualifiedRecords=[]`和真实 TLS/device/qualification 证据仍缺失，不产生 capability/readiness 宣称，也不改变默认 shipping Relay v1 事实。

该 handoff 前另有独立、default-off、injected-only 的 bootstrap byte-source adapter。它在构造期只捕获并 claim 注入 source，同时只捕获 H-Cred2 privileged intake，首次 one-shot read 才启动 iterator；adapter 只拥有单条最多 8192-byte payload、可选 terminal LF 的有界 framing/ASCII preflight、source cancel 与 close/drain，并在完整 EOF 和 cancel 成功后才同步移交 H-Cred2。公开 frozen handle 只有 `readCandidate()` 与 `closeAndDrain()`；失败或关闭后不重开、不重试，也不关闭或消费 H-Cred2/Vault。该adapter可由privileged intake接收外部已拥有的source，但intake不选择stdin/file/native loader或capability advertisement，只经上述 default-off shipping root 的显式选路可达；没有注入source时只恢复已有cell，不改变默认shipping Relay v1事实。

同一 credential vault 另有独立、default-off 的 Host credential atomic file cell foundation。它只接受显式注入、已存在且 canonical 的绝对目录，并要求目录非 symlink、当前 euid 所有且精确 `0700`；固定 credential 与 claim 名称，在读取 credential 前以持久 claim 排他占用整个 cell 生命周期，并以 no-follow open、`lstat`/`fstat` identity、owner、精确 `0600`、单 hard-link 与 Vault canonical byte limit 校验数据。CAS 以同目录私有随机 temp、file fsync、commit 前复核、原子 rename 和 directory fsync 发布；rename 后验证或 durability 失败会返回 uncertain 并永久 fence，close 只按 owner token 与 inode 清理本实例 claim/temp，不删除 credential。该 4a foundation 未接 Vault/Authority、`relay-host` 或任何 production composition，不产生 capability；崩溃遗留 claim 固定要求显式 recovery，descriptor-relative TOCTOU 消除、可审计 orphan recovery 与 rollback-independent continuity anchor 仍属于 4b，也没有真实跨进程崩溃、power-loss 或 rollback 证据。

同一 owner 边界现另有一个独立、default-off、只消费显式注入 fake/raw 同步 Host ABI 的 closed Node wrapper foundation，以无字段 owner-bound revision 实现既有 AtomicByteCell 并由稳定 `closeAndDrain()` 唯一收口 raw handle；它不读取 HOME/path/env、不加载 addon、不接 Vault 或 production composition，真实 native/Rust/syscall、recovery、durability qualification、continuity、loader/packaging 与生产接线仍属于 4b2–4b6。

相邻另有独立、default-off、injected-only 的 Host native credential module one-shot source/holder foundation（`src/relay/v2/hostCredentialNativeModuleSource.ts`）。它只持有来自自 revision 6 冻结、revision 7 保持不变的 `nativeArtifact.supportedTargets` 的 contract-fact target descriptor（`darwin-arm64`/`aarch64-apple-darwin`、`darwin-x64`/`x86_64-apple-darwin`、`linux-arm64`/`aarch64-unknown-linux-gnu`、`linux-x64`/`x86_64-unknown-linux-gnu`），由 `src/relay/v2/hostCredentialNativeTarget.ts` 单一事实源投影，descriptor 不含任何 artifact specifier、路径或 loader-relative 身份；capability 只表达 exact target/platform/contractRevision 7/ABI（napi、abiVersion 1、最低 napi 9），永不表示 readiness 或 qualification。加载完全由注入 loader 完成且每个 source 最多一次并 memoize；selection-in-progress fence 使 loader 回调期间的 re-entrant probe/take 以非 memoized invalid fail closed、loader 内 re-entrant close 在回调返回后任何存储前重新检查并只 memoize invalid。ABI admission 只接受仅含 frozen 同步 open 方法的 exact own-data record，拒绝 Proxy、accessor、symbol key、extra/missing/async 方法、array、own/inherited thenable 与 non-plain prototype，并原样返回原 receiver；one-shot `takeNativeModule()` 把 exact module identity 一次性移交既有 injected bridge，交付后不可重取，close/失败有界回收并以稳定 redacted error fail closed。它不从 env/argv/任意路径/HOME discovery/JSON/dynamic plugin 加载，不运行时探测生成 qualification，也不接 `relayHost` 或任何 composition。native artifact identity 自 revision 6 冻结、revision 7 保持不变（descriptor/digest/layout/loader 输入规则）：`src/relay/v2/hostCredentialNativeTarget.ts` 是该 contract 的 JS 单一事实源（四项 fixed target descriptor），`src/relay/v2/hostCredentialNativeLoader.ts` 的 fixed loader 只按 exact target 映射唯一 frozen loader-relative specifier，只有 fixed artifact 在 resolve 阶段确实缺失才映射 `native_artifact_missing`，其余加载/binding 失败都 redacted；它是可被 trusted deployment 显式选择并注入该 holder 的窄 source。Host 专属 N-API crate 与本机 target build/stage/pack verification foundation 也已存在（见 `native/` 表），但仍 default-off、未接线。该 foundation 不产生 qualification、readiness、capability 或 productionWired；durability `qualifiedRecords=[]` 仍为空。Revision 7 起同一 loader 模块另提供 fixed trusted loader（见下一段），trusted factory 与 pre-bound directory descriptor capability 已作为 default-off 未接线 foundation 存在。

相邻的 trusted native capability factory v1（顶层 contract revision 7 additive）已闭合为同一 foundation：raw N-API artifact 保持 frozen v1 exact 单 `open` 表面（既有 fixed loader/module source/wrapper 可观察行为不变），raw factory `createRelayV2HostCredentialAtomicFileCellTrustedFactoryV1` 只作为 additive own-data entry 存在于 raw open function 上——不声称可见性隔离，只冻结其唯一 production driver/consumer。fixed trusted loader 按同一 fixed target 规则 resolve/load 后捕获该 entry，在初始化时 exactly-once 无参调用（带参数固定 `INVALID_ARGUMENT`，进程内第二次固定 `CELL_CLOSED`）；native producer 由 Darwin/Linux adapter 各自实现，先证明 real==effective 且非 root 的 credential snapshot，再只经 getpwuid 族 account-database home proof（绝不读 HOME/env/argv/JSON/global lookup），拼接 platform-common build-time 单点消费的 contract 固定组件 `.tmux-worktree/relay-v2-host-credential-atomic-file-cell-v1`，no-follow 逐层 secure open 取得 exact owner/no-symlink/exact `0700`/`FD_CLOEXEC` directory descriptor，不创建/chmod/修复/选 alternate。成功 mint 的不可伪造 capability 只以 Rust 值存在，从不暴露给 JS 或 raw fd number；one-shot binder 消费后只返回 exact own-data `{open}` 的最终 v1 module，factory 与 open 不在同一最终对象并列。binder replay/rollback/PID-fork/final close 全部 fail closed：descriptor exactly-once raw close 不重试、不 dup/reopen；PID-fork 后一切 operation（含最终 module 的 `open`）先经 capability origin PID fence 固定 `CELL_CLOSED`，fork child 绝不 take/read/adopt/close inherited descriptor。最终 module 的 `open` 与 raw v1 module 共用同一 frozen gate：空 `qualifiedRecords=[]` 下仍 `CELL_DURABILITY_UNSUPPORTED`，在 registry/descriptor adopt/mutation 前失败；未来 adopt 只进入 platform-common 唯一 `AdmissionOwner`/registry，本 factory 不新增 mutation owner/registry。显式 `relay-host --profile v2` 的唯一 trusted deployment source 现已选择该 fixed trusted loader/source；qualification、recovery与真实 native/TLS/device 证据仍未实现，因此实际 qualified open 在任何 mutation/socket 前 fail closed，整体 NO-GO 不变。

该 Host native contract 顶层已升为 revision 7；raw ABI、native fixture、platform resource contract、claim journal format 与 credential mutation contract 仍分别为 v1，native artifact identity 也保持 revision 6 冻结的 v1，revision 7 只 additive 新增 trusted factory v1（四项 fixed target descriptor、binary/digest/layout/loader 输入规则与 fixture，见 `contracts/relay/v2/host-credential-atomic-file-cell-v1`）。Host 专属 platform-common `AdmissionOwner` 继续把已预绑 directory descriptor 收口到进程内 directory identity registry、sole lock descriptor、`TWV2HAC1` fixed claim journal 和 close owner，并在同一 owner 内持有 mutation state；directory/lock/claim 与credential/temp操作分别来自不变的 admission trait和additive mutation子trait。它在接管directory并通过parent PID fence后、所有registry与namespace mutation前以OS CSPRNG一次签发exact 32-byte non-zero opaque claimId；source error或全零只返回`CELL_IO`并raw-close directory一次，caller不能提供该值。它以 opener PID fence 禁止 fork child cleanup，以 Host 专属 `(directory dev, ino, logical kind)` 在第二 lock fd 前拒绝同进程 duplicate，只允许 nonblocking traditional process-owned whole-file `F_SETLK`。Mutation每个phase另要求process registry中的exact token/opener/fence仍为`Open`，不能复用允许`Closing`的close检查；opaque revision绑定该lifecycle fence、owner generation与credential observation，新read/conflict会使旧revision stale。Normal close只在没有terminal mutation fence或known temp、且 exact claimId/journal 与 fd/path identity 复核成功后 unlink claim、fsync directory、各 raw-close claim/lock/directory 一次；fenced/known-temp close不枚举或恢复，只raw-close已持有descriptor并保留claim/temp。Claim journal v1 继续只表达 `ADMISSION_HELD_NO_CREDENTIAL_MUTATION` admission proof，不记录revision、temp或mutation phase。该 crate 与 broker N0 的 namespace、path、binary/journal format、registry 和 owner 完全分离，不依赖其任何 crate。

Revision 6 的 credential mutation v1 已在 platform-common 的同一 `AdmissionOwner` 内实现，Darwin/Linux adapter也分别实现所需descriptor-relative syscall seam；revision 7 的 trusted factory final open 现只在 qualification 成功后把 prebound descriptor 移交该 owner，并发布 frozen v1 read/CAS/close handle，但 durability qualification 与 production 可达路径仍缺失。common owner要求 exact live owner 在read与CAS每个phase重验parent PID、registry exact-Open fence、directory/lock/claim identity与claimId；read固定执行credential `fstatat` nofollow→`openat` nofollow/cloexec→A/B/C stable proof，只有initial `ENOENT`为absent，present必须是current uid+gid、regular、exact `0600`、`nlink=1`且不超过65536 bytes。Opaque one-shot revision绑定owner lifecycle fence/generation、absent/present tag、exact bytes digest与present object identity；CAS先consume exact revision，并在temp前和rename前检查current两次。Temp固定为同目录前缀加32-byte CSPRNG的64位lowercase hex，使用`O_EXCL|O_NOFOLLOW|O_CLOEXEC`、`0600`且只对`EEXIST`最多重试8次，碰撞对象不清理，已创建对象只按tracked dev+ino清理；write/fsync/proof、owner revalidation与第二current check后，same-directory `renameat`是唯一commit point，随后还必须完成temp absent、published A/B/C identity/bytes proof、directory fsync与close。Rename成功后的任一失败，或rename错误却无法同时证明旧值未变与temp仍exact owned，均返回uncertain并永久fence；明确未提交但cleanup identity/durability不确定则返回`CELL_IDENTITY_UNCERTAIN`或`CELL_RECOVERY_REQUIRED`并永久fence。它不提供crash recovery或enumeration cleanup，遗留claim/temp继续preserve并要求recovery。纯`credential-cell-read-cas-temp-or-rename`与N-API handle projection实现项已经完成；qualification与production/admission/durability/wiring/capability边界仍为空或false/none。

Host 专属 Darwin 与 Linux adapter 已实现 admission 与 credential mutation 的 descriptor-relative traits；真实验证分别覆盖 `aarch64-apple-darwin` 与 `x86_64-unknown-linux-gnu` 的 descriptor-relative filesystem、credential read/temp/write/fsync/unlink/rename、独立 subprocess 间 `F_SETLK` busy 与 raw-close release，以及 exec 边界 `FD_CLOEXEC`。Darwin x86_64 与 Linux arm64 尚无证据。该 contract 的 durability `qualifiedRecords=[]`，production 无法构造 qualification，完整 admission adopt 因而仍不可达；上述 target 证据不等于完整 admission、recovery、filesystem power-loss、N-API/loader、Vault/Authority、`relay-host` production wiring、readiness 或 capability，也不产生 H4a/Relay v1 fallback。

相邻的 credential-exact Host WSS lifecycle factory 是该 injected port 的独立 default-off 实现。credential authority 为一个 controller attempt 签发 owner-bound one-shot admission；HostCarrier 从同一 cut 只取得 exact reference/version/grant/JTI metadata，lifecycle 在 bind 前再次验证 cut 仍为当前版本，再让 authority 通过 lifecycle 捕获的 write-only request finalization port 写入一次 Authorization Bearer 并结束实际 handshake request。token 不进入 WebSocket constructor options、headers record、URL、subprotocol、公开对象或日志，credential authority 也不构造 socket。lifecycle 只把 root `wss://` 转为 `/host`、只提供 `tw-relay.host.v2`、关闭 per-message deflate，并拥有 raw-byte bounded pre-open FIFO、binary frame limit、write-callback ACK ordering及close/drain late-callback fence。现有 shipping/bridge/intake options 另透传两个彼此独立、可分别省略的 injected-only CA-only trust record，分别属于 credential issuer HTTPS 与 carrier WSS；每层只接受并复制有界非空 CA list，省略即为该 lane 的 system trust。两条 transport 都固定 `rejectUnauthorized=true` 与 Node `checkServerIdentity`，该 port 不能携带 client cert/key、验证开关、hostname verifier 或任意 TLS options。测试只为 WSS 注入 fake WebSocket，HTTPS 仅有本地私有 CA 行为证据；该切片不新增 deployment source、profile/CLI、qualification、capability、fallback 或 production TLS/process evidence。

Registered HostCarrier control 另有独立未接线 adapter：`RelayV2HostCarrierActor` 在自己的 bundle closure 内创建并 native-private 验证唯一 owner，再直接构造只持有该 owner 的 adapter；不会把可伪造的结构化 port 或原始 sender 交给 adapter entry，也不依赖跨 entry class brand、`Symbol.for` 或 global registry。HostCarrier 继续独占 connector generation、hostId + connectorId fence、唯一 pending correlation 和既有 bounded control queue/control reserve；authority 把 gate 的 exact hostId + connectorId 带入副作用，create 只发送固定 300000ms 的 `enrollment.create`，revoke 只发送固定 `user_revoked` 的 `grant.revoke`。相同 pending fingerprint 只复用一次发送，不同请求返回 BUSY；replacement、disconnect、supersede 或 dispose 同步撤销旧 generation，malformed/unknown correlation fail closed。该 adapter 只投影一次性 enrollment code 或非敏感 revoke receipt，不拥有 sender、connector、queue、retry、deadline、持久化或 v1 fallback。

Node 侧现有一个 default-off 的 Dashboard management v2 composition owner。它不再分别接受可能错配的raw Host connector controller与raw HostCarrier actor；唯一输入是protocol-v2 session从上述Host port取得的pending closed binding，以及同一canonical credential authority/exchange coordinator和固定Host identity/credential reference。composition先完成credential owner/adapter等可前置构造，再pending consume binding取得窄connector lifecycle与动态exact-current registered carrier control；只有三个真实adapter、串行management authority、request/drain handle和activation record全部可发布后才commit Host transaction并登记exclusive claim。任一构造或commit failure都会exact abort，旧binding永久失效，满足无generation/lifecycle副作用条件时恢复原port与公开lifecycle；binding copy、bound copy、Proxy、foreign identity、replay、旧generation/replacement/close或credential authority failure均fail closed，且不保留第二套raw WeakMap claim。公开handle只有request与`closeAndDrain`：close先永久fence新请求、等待已接纳management request收敛，再只通过committed closed binding的exact `stopAndDrain`等待connector drain，不直接关闭transport或复制lifecycle。独立的default-off session owner是Host management port的唯一领取者；它只在owner validation和transaction commit成功后发v2 ready，EOF、坏帧64、handler/write普通失败、abort或显式close均先永久fence新请求，再exactly-once等待composition drain。refresh只更新credential authority cut，不隐式触发Host reauthentication。canonical Host production composition 的显式可选配置是该 session 的唯一 composition callsite；省略时不领取port且facade不变。Tauri bundled management supervisor与hidden Node CLI固定使用exact management protocol v2。hidden child的selection owner现只把自身stdio IO、runtimeVersion、clock和同一个child-owned signal交给唯一Host trusted deployment opener；该opener以同一signal完成activation/startup，并在child进程内发布一个explicit-v2 shipping root。root经既有`dashboardManagement` seam把channel交给同一canonical credential/controller/carrier composition并条件暴露`runDashboardManagement`，所以`start_connector`与`enrollment.create`不再停在selection层。child不接受raw trustedHome/deployment/runtime，也不采用独立`relay-host --profile v2`的auto-start/retry process lifecycle；这使hidden child成为该进程唯一root/handle owner而不产生竞争Host进程。session先排空management/controller；随后root依次关闭reconcile lifecycle、intake/canonical及trusted runtime/native activation。handle发布前失败或合法handle无management且clean close仍收敛到唯一typed `UNAVAILABLE`，session/close不确定或非法handle仍ordinary exit 1，绝不启动第二session或v1 fallback。Host `qualifiedRecords=[]`与E0/真实TLS/device证据仍缺失，当前child因此仍在native mutation/socket前fail closed，不能创建可用enrollment或宣告capability/readiness；默认生产Relay runtime仍只有v1。

### macOS 后端：模块化 Rust/Tauri

`app/src-tauri/src/lib.rs` 只是 composition root：注册模块、进程级 state 和 Tauri commands，并处理应用退出清理。业务实现分布在：

| 路径 | 职责 |
|---|---|
| `config/` | projects、hosts 和跨进程配置写锁 |
| `features/control_plane/` | bundled/installed TW 发现、local/remote RPC、capability 和 lifecycle 路由 |
| `features/sessions/` | local/remote catalog、tmux identity、activity 和 legacy discovery |
| `features/worktrees/` | worktree 创建/恢复委托、orphan 发现与清理 |
| `features/terminals/` | managed terminal 委托和 Dashboard terminal registry |
| `features/pty.rs` | 本地与 SSH tmux attach 的 PTY 生命周期 |
| `features/terminal_control.rs` | Dashboard PTY 到 canonical terminal-control 的受控 input/resize/lifecycle adapter |
| `features/feishu_bridge.rs` | Feishu product daemon 的按需启动、binding 管理和持锁 handoff adapter |
| `features/git/`, `features/files.rs` | local/remote git 与文件能力 |
| `features/layout.rs`, `features/automation.rs` | Dashboard 持久化和 automation 执行 |
| `features/mobile_relay/` | broker 安装、connector/serve 子进程、配置和状态 |
| `ipc/` | Tauri command DTO 和 TW RPC response model |
| `remote/` | SSH command transport 与 Host model |
| `support/` | 原子文件、环境、process 和 shell helper |

Rust 可以直接做 catalog 查询、PTY、git、files、布局和 orphan 清理；它不能在 canonical TW runtime 不可用时悄悄改用另一套 git/tmux creator 创建 managed session。

### Android：独立原生客户端

Android 入口是 `V2Activity.kt`，产品 UI 位于 `app/`、`feature/` 和 `designsystem/`，数据与连接能力位于 `core/`：

```text
Compose screens / navigation
           |
           v
       V2ViewModel
       /    |     \
      v     v      v
  Room repo DataStore RelayV1ConnectionActor
      |        |             |
  cache/outbox prefs       OkHttp WebSocket
           \
            Android Keystore credential store
```

- Room 是 Host、Scope、Session snapshot、Timeline、Outbox 和 stream checkpoint 的手机侧持久缓存。
- DataStore 保存非敏感 profile/preferences；Relay secret 由 Android Keystore 的 AES-GCM key 加密后保存。
- `RelayV1ConnectionActor` 用单线程、有界队列串行拥有 socket、request 和 terminal stream 状态。
- `TerminalWebView` 加载 APK 内置 xterm assets，不依赖 CDN。
- `MainActivity.java` 只是旧安装恢复 activity class 的跳转 shim；launcher 和产品逻辑都在 `V2Activity`。

Android 的 `V2` 指当前产品/UI 代际，不表示它已经实现 Relay v2。

Android Relay v2 Outbox 的 existing core + Room repository 现在另实现一个独立 internal durable enqueue port。caller 必须提供已经稳定的 exact activation namespace 与完整 draft；repository 在一个 Room transaction 内 strict decode current state、执行 `RelayV2OutboxAction.Enqueue`、验证 activation namespace 与新 row 的 exact host lineage并只应用其唯一 mutation plan，commit 后才返回仅含 host lineage、commandId 与 creation order 的非敏感 correlation。authority namespace 不含 host selector，同一 activation 可持有分别绑定不同 `hostId/expectedHostEpoch` 的 rows。duplicate、capacity、foreign activation、corrupt/unknown state 与 store failure均 fail closed且不返回 receipt。Production root 把该 port 接入受限的 Session reply、已 admission 的 existing-Session kill，以及每个当前 materialized、UI 可见 Scope（包括空 Scope）的 durable `create_worktree` / `create_terminal` producer。reply/kill 消费 issuing composition 的 Room opaque exact Session cut；两类 create 只消费同一 composition 从当前 committed Scope projection 签发的 opaque exact Scope cut，其 authority 绑定 exact activation/state namespace、`scopesRevision`、Scope 与 hostEpoch，UI raw scopeId 仅用于查找该 cut。四条路径都在 actor-issued current ONLINE lease 与唯一 product mutex 内重验 Room materialized exact authority；create 还会在 Room read 前额外重验 current projection 中的 exact Scope cut；reply 固定构造 `send_agent_message`（`pane=0`、`submit=true`），kill 固定构造 `RelayV2OutboxArguments.KillSession`，两类 create 分别构造 canonical `RelayV2OutboxArguments.CreateWorktree` / `CreateTerminal` 并提交各自 `sessionId=null` 的 exact create lane。Room commit 后才调用既有 fresh producer/dispatcher；reply 只有 captured composition/exact cut仍 current且 raw draft未变化才清 draft。kill queued 不等于 terminated且不乐观删除 Session；create queued 不等于 created，不本地插入或导航 Session，两者都等待权威 `sessions.changed` delete/upsert。Session projection 与所有 recovery materialized-state mutation共用唯一 product mutex，最终发布还在 actor lifecycle lock 内重验原 ONLINE cut；UI route/list identity使用不可碰撞的本地编码。server/host default-shipping v2 capability、externally usable enrollment/terminal interoperability 与额外 extension command producer 仍未交付，且此路径不产生 ready/capability或 v1 fallback。

Android production root 另有严格受限的 Relay v2 base-sync 分支：cold-start admission 只有在已有显式 v2 profile 与其 credential 精确验证后，才把同一个已验证 profile 交给 `RelayV2BaseRuntimeComposition`。该 composition 独占一个 `RelayV2ConnectionActor`、基础 state-sync effect pump 与 close lifecycle，复用 `AppContainer` 的同一 v2 credential store 和 Room state repository，并以 `BoundedRelayV2TransportFactory` 作为唯一 WSS 入口。它只处理 hello/query-or-resync、snapshot/release/expiry、`scopes.changed` 和 `sessions.changed`。同一 composition 只对 exact retryable connection failure 使用一个 capped exponential timer；每次 successor 由同一 actor 在旧 transport/apply drain 后启动，Room Outbox snapshot 逐连接重读并以 opaque attempt identity 绑定 exact generation，只有 actor `OnlineReady` 重置 backoff，close/cancellation/disconnect/terminal failure 会先 fence pending retry；owned backoff 对 base state/V2ViewModel 投影为 CONNECTING，同一失败 generation 的迟到 actor FAILED 不能覆盖，non-retryable 与 RuntimeIncomplete 才保持 terminal FAILED；显式 profile 的 STOPPED/CONNECTING/RESYNCING/ONLINE/FAILED 只是连接状态，不表示 Relay v2 ready 或 capability。

仓库已有相互独立的 Node 与 Android Relay v2 codec，并共同消费 `contracts/relay/v2` 的 strict framing、closed schema、limit、dialect 和 normalized-result fixture。Node 侧还包含未接线的 Broker/Host carrier foundations、H0 host 事务状态库与 H1 command-plane core：H1 在 H0 事务上持久化 `(hostEpoch, principalId, hostId, commandId)` scoped ledger、dedupe window、durable accepted runner、结果/tombstone；`clientInstanceId` 只记录首次 route/attempt，不参与 ledger identity。四类固定 mutation 只通过注入的 `tw rpc` / terminal-control executor seam 执行；create/kill 的成功终态还要求注入 H2 resource mutation owner，在同一 H0 transaction 内签发/解析 opaque Session identity，并提交 Session mapping/revision/eventSeq 和 ledger。这些 foundations 尚未接入 `relay-server` / `relay-host` composition root，不生成 credential/enrollment，也不宣告 v2 capability。create target 的 target-side catalog observation/revision/fence 另有显式 default-off foundation：独立 versioned companion ABI `contracts/tw-rpc/create-target-observation-v1`（不扩展 frozen TW RPC v2、无 capability advertisement、无 fallback）定义 observe/admit 两阶段——observe 由 target 侧 `tw create-target-observation-v1` entrypoint 原子观察闭集 catalog（config project、canonical realpath、canonical git-dir/common-dir identity digest 与 exact resolved base-ref OID）并返回覆盖全部观察输入的确定性 `twcat1.*` revision；admit 在同一进程内先现场重新观察并对 revision、arguments 与闭集 execution 做 canonical exact 比较，观察输入闭集内的漂移（含 base-ref OID 或 git identity 漂移）以零副作用 `OBSERVATION_STALE` fail closed，完全匹配才紧接着执行 canonical resolved mutation（no retry、no fallback、不解析 SSH 目标），已证明边界为 observe→admit 之间 target 侧的 reobserve exact 比较，残余窗口仅为 reobserve→mutation 之间不可约的进程内本地 TOCTOU（与任何本地 create 同级）。Relay 侧 observation authority/store 保留在独立 authority module，由 admission 模块的 pair issuer 构造唯一实例并封入 record；claim wrapper、admission adapter 与 pair registry 共置 admission 模块，两个组件是模块私有 class，无公开 constructor 或 registry opener；唯一入口 `issueRelayV2CanonicalCreateTargetExecutionPairV1({ owner, runner, inner, ... })` 对所有 options 做一次 exact own-data 捕获（accessor/Proxy/symbol/多余 key 同步 fail closed）后打开 owner 的 same-owner bundle、构造唯一 observation store，并只返回 frozen null-prototype 空 ticket；私有 wrapper 与私有 adapter 延迟到一次性 H1 factory 内构造（base non-create process 在签发时封入），组件不可取得、hybrid 不可表达。Host canonical composition 以唯一 `createTargetExecutionPair` option 接收 ticket，在 hostState.read/H2/任何 lookup 前经 `captureRelayV2CanonicalCreateTargetH1FactoryV1` 同步 exactly-once capture（foreign/复制/replay 在该处 fail closed），所得一次性 lexical closure 被调用时对 input 做一次 exact own-data 捕获、装配 command target resolver/executor 并直接 openRecoveredAuthority，只返回 opaque readiness candidate；evidence 携带 catalogRevision，fence 同步一次性消费，admitted store 以不碰撞 opaque authority token 为唯一键（token 保留 per-实例唯一 digest 输入）、按逐-evidence exact identity（operation/arguments/闭集 execution 与携带 fenced command fingerprint 的 reservationCorrelation）恰一次消费，相同 execution 的并发 evidence 各自独立成行，create_terminal 的 admitted arguments 与 executor mutation 共享同一 exact canonical 形式（canonical cwd + derived label)。create lane 之外的 operation 仍走既有 rpc-v2 structured lane；这些 owner 未接入 production composition，不宣告 capability，也没有 qualified 真实设备证据。

Node侧另有一个default-off、仅由显式activated Broker server composition采用的broker client WSS runtime composition：它在同一闭包内装配现有 Broker Core/client transport、authorization-expiry deadline owner 与 transport close coordinator，并在 HTTP 101 前通过 Core 的 `inspectClientAdmission` 把 offline、dialect/capability、expired/fenced 与 composition closing 闭合为冻结的 401/403/426/503；成功 preflight 只签发 module-private、composition-owner-bound、one-shot opaque receipt，exact 绑定 connectionId、可信 auth snapshot 与 Host producer target。已完成 HTTP 101、精确协商 `tw-relay.v2` 且无 extension 的 socket 只能消费该 receipt 进入既有 Phase B attach；copy/proxy、foreign、replay、stale 或 close 后 receipt 都在触碰 socket/close lease 前拒绝，随后 `openClientRoute` 仍执行第二次 admission 以关闭 101 后 route race。close coordinator 为成功进入 Phase B 的 managed client socket 签发 one-shot opaque incarnation lease，transport 只消费该 exact lease。native close/error 先经 adapter fence 后 exactly-once unbind Core，再撤 expiry、以真实 terminal 注销 close lease并等待 adapter/监听清理；expiry、revoke/kid 与 authority-loss 复用 Core fence及 coordinator 的 4401/4403/1013 + 5s force-destroy policy，force 请求本身不构成 drain 证据，replacement/late callback 继续按 exact incarnation stale。composition 只额外暴露 future Host Pump 所需的 bound narrow Broker authority，不暴露 receipt 内容、完整 Core、transport、coordinator、expiry owner 或 adapter；它不解析 Authorization、不创建 listener/socket/HTTP 101，也不拥有 credential/E0、retry/fallback、capability advertisement；默认CLI不注入activated composition，`qualifiedRecords=[]`与缺失qualified E0/TLS证据使其不可用，因此不改变默认shipping Relay v1事实。该 client socket transport 现另执行一次性握手阶段 deadline 机 `awaiting_relay_welcome → awaiting_client_hello → awaiting_host_welcome → complete`：已送达并经 Core ACK 的 `relay.welcome` arm 5 秒 client.hello deadline，首个被转发接受的 `client.hello` 切换为 10 秒 host.welcome deadline，已送达并 ACK 的 `host.welcome` 取消并完成；到期以 4408 `handshake_timeout` 关闭，scheduler install 或 transition-time cancel 失败 fail closed 为 1013 `client_handshake_timer_failure`。timer 以 token 绑定防止 stale callback 关闭 replacement，`beginBoundedClose`/`finalize` 清除 deadline，重复或迟到信号不重启一次性 deadline；该 enforcement 完全在 101 后 socket 层，不改变既有 pre-101 admission，也不新增 retry/reconnect 或 v1 fallback。

此前的 BAU path/JSON authority/continuity 设计直接处理 state/lock path、fd/inode、rename/unlink 与 cleanup，未通过 native 安全验收，因此没有纳入当前交付源码，也不得作为 fallback 重新引入。`contracts/relay/v2/broker-credential-state-store-v1` 另行冻结唯一 `RelayV2BrokerCredentialStateStore` deep port、exact caller-owned `trustedHome` open、transaction-scoped opaque revision、closed native capability/open/error union，以及 single descriptor-backed binary v1。其顶层 contract已是 revision 2，但 N-API interface、storage/binary、header、private-location derivation与fixture仍为 v1，artifact/ABI/bytes不变。Revision 2冻结 native account-home/mode/ACL secure-open、qualification-before-mutation、final A/B/C proof、traditional process-owned `F_SETLK`、platform-common registry/PID/final-close语义，以及 deny-by-default durability policy；`qualifiedRecords=[]`，所以所有真实 open必须在 registry/mutation前 `DURABILITY_UNSUPPORTED`。该 container 固定 fileLength/absolute header+payload offsets，只允许 positional payload barrier → positional header barrier publication；sole descriptor lock持有到 exactly-once final close，不使用 lock file、shared cursor、named replace/rename 或 cleanup。`src/relay/v2/brokerCredentialStateStore.ts` 只提供 interface 与 raw N-API store/transaction/revision/bytes closed decoder/wrapper；它复制 snapshot/input 并在 uncertain 或 native protocol violation 时 terminal-close wrapper instance。`src/relay/v2/brokerCredentialStateStoreLoader.ts` 只在 pre-open 边界按显式 target、最低 N-API 和固定 artifact 映射 optional-load 一次 binding，并把 capability/open 原样交给上述 closed wrapper；只有固定目标 artifact 自身在 resolve 阶段确实缺失才映射 `native_artifact_missing`，其他加载、binding、capability 或 open 异常都保持 invalid。它精确传 caller-owned absolute `trustedHome` 与冻结上限，不产生 overall ready。`native/relay-v2-broker-credential-state-store-core` 独立消费 frozen binary fixture，纯核心只负责 bounded range selection、owned FIFO admission/transaction lease、opaque revision、same-bytes precedence、generation exhaustion、四阶段 publication、uncertain terminal poison 与 close barrier；fixture JSON/Base64 materializer仅存在于 tests。未接线的 `native/relay-v2-broker-credential-state-store-platform-common` 由 build script 单点消费 manifest 的 canonical private location并生成 immutable container spec；runtime是Darwin/Linux adapter与N-API binding对N1的唯一shared lifecycle owner，拥有显式 eager process-lifecycle token、pre-fd registry typestate、PID/descriptor fence、panic-aware永久poison、common-owned exactly-once final-close bridge和opaque process-bound store/transaction wrappers。Platform attach后按值交出sole container；N1 raw store/ticket/lease/revision/snapshot不外逃。`from_adapter`失败保留primary并受控close，ordinary close先封registry admission再让N1 drain，publication failure仍由N1闭合为`Uncertain`且不被same-PID registry poison覆盖。该owner本身不接`trustedHome`、不构造path或实现syscall/filesystem/durability adapter。未接线的Darwin/Linux adapter foundations通过该owner提供target public open seam；未接线的N-API binding foundation在module init eager exactly-once捕获process token，只把`ProcessBound*`与所选platform open seam转换为冻结raw ABI，不拥有credential schema、readiness、continuity、fallback或capability advertisement。空qualification allowlist使platform-adapter-qualified real open与actual opened JavaScript transaction均不可达。显式opt-in foundation现可按exact target triple执行locked release Cargo build、校验Mach-O/ELF header、只原子stage到loader fixed specifier对应路径，并在自建临时目录中以`npm pack --ignore-scripts`验证tar/unpacked layout和现有closed native-binding test；它未加入普通build/npm lifecycle/production composition。当前证据仍仅是Darwin arm64；没有Darwin x64/Linux、四target matrix、bit reproducibility、Dashboard bundle、签名/notarization、minimum-OS/glibc/SDK、provenance、qualified real open或真实filesystem/kernel lock/durability/fork/power-loss证据。未接线的 `RelayV2BrokerCredentialAuthority` source foundation 已通过 opaque port 独占 versioned issuer/enrollment/grant/replay/rate-limit、ready withdrawal 与 external continuity 业务语义。Credential authority 的 enrollment create/redeem、client/host refresh、host reauthentication 与 host bootstrap API，以及严格的 B1 host-bootstrap HTTP ingress adapter，均已在隔离模块和专项测试中可调用。B1 只通过窄port做method/path/header/body admission、bounded read、closed decode/response mapping与authority调用；它与B4已由显式default-off activated composition及public `node:https` lifecycle root采用，但默认CLI不调用且qualified E0 activation不可达，因此仍没有qualified/default-shipping外部生产HTTP路径，不产生ready/capability，也不能宣告已经交付。现有未接线的 live-authorization/fence foundation仍由 credential authority拥有durable事实：grant revoke或kid removal在publication前先begin exact invalidation，只在durable commit后携带单调revision/fence settle；缺失/失败的begin不publish，publication uncertain只fail closed并withdraw authority。BrokerCore拥有active admission/fence，使用trusted time与closed 11-field authorization snapshot，在attach、auth-control、route和逐帧dispatch复核expiry、role/host binding、pending/committed exact identity fence；authority首次withdraw通过同一窄port同步全局latch，既有连接只发once-only typed symbolic close reason和connection incarnation；transport close coordinator 以 exact kind+ID+incarnation 消费该 signal 并实现 4401/4403/1013 与 5000ms force-destroy policy，已由上述default-off composition采用；public root已拥有listener/WSS lifecycle，但仍无qualified real public TLS/network/device或ready-loss证据。同一 credential authority 还拥有未接 production composition 的 response replay-key rotation lifecycle：私有 envelope v3 使用 bounded active + decrypt-only keyring 和永久有界 identity history，record exact 绑定 key id，旧 v1/v2 单 key state/ciphertext/TTL 无损迁移，rotationId 幂等且 unknown key、AEAD 或 malformed state 按 fatal path withdraw；它没有 production 管理入口，也不改变 public wire 或 ready/capability。port 不接收或暴露 storage/lock/temp path、fd/inode、`*at` 或 cleanup。Capability supported 和 opened+self-check 都不等于 ready；existing unknown/corrupt/unsafe/identity/durability failure 是 preserving invalid，不是 missing/unsupported。当前仍没有qualified/default-shipping authority activation、Dashboard/production shipping或continuity readiness；native foundations unavailable或durability unqualified只表示v2 unavailable，不能进入BAU设计或v1 fallback。因此production v2继续disabled，Relay v1 build/runtime不依赖native module。

独立、default-off 的 Broker access-expiry deadline owner 只用 exact kind/ID/incarnation 进入同一 serialized Core cut，并仅按 Core 返回的 closed `active.expiresAtMs` 绝对调度；expired/stale/fail-closed、replacement、unregister/close 和 scheduler/cut failure 都在 owner 内有界收敛。同一 owner 现按到期前 60 秒 lead 对每个 credential 各发一次 warning：Host 侧经 carrier control queue 发送 `host.auth_expiring`，client 侧经 client queue 在保留的 `relay.welcome` 之后发送不占 host eventSeq 的 broker control event `auth.expiring`（`refreshRecommendedAtMs` 恒为 `expiresAtMs - 300_000`）；once/exact-current/rearm 语义与 Core 侧 exact-incarnation warning cut、deferred 重放和单连接 slow-consumer 1013 关闭共用既有路径。它已由显式default-off activated Broker composition采用，但默认CLI不注入；它不改变现有 close coordinator 的 4401/5000ms policy。

B4 在同一 credential HTTP boundary 上增加 enrollment redeem、client/host refresh 与 self-revoke 四个冻结端点；credential/replay/rotation/revoke fact 仍只由 `RelayV2BrokerCredentialAuthority` 拥有。该owner只由显式default-off activated Broker composition与同一credential authority采用；默认CLI不注入且qualified E0/native仍不可用，因此不形成shipping credential、ready或capability。

`contracts/relay/v2/external-continuity-authority-v1` 现已冻结future external authority的closed interop/security语义，但production adapter仍为NO-GO。external backend只拥有按`anchorId`的rollback-independent durable monotonic linearizable record和terminal lifecycle；continuity anchor继续拥有local-state-before-anchor ordering、唯一一代crash reconcile与bounded operation timeout，credential authority继续拥有credential state、ready withdrawal与closed error mapping，BrokerCore消费auth-control authority与上述窄live-authorization fence port。future composition唯一顺序是native store open+self-check → external authority injection → credential authority open/reconcile → synchronous admission/active-data fence → broker injection。独立未接线的Node HTTPS adapter foundation现只实现exact endpoint POST、system TLS/hostname verification、caller-injected workload auth header、no redirect/compression、双向`Cache-Control: no-store`、pre-body status/header gate、16384-byte counting reader、strict closed outer decode和read/CAS error闭合；success inner仍原样交给continuity owner，operation deadline也只由continuity owner传入的signal控制。其上的default-off closed config binder严格捕获冻结的reference-only config与1..2项唯一namespace/anchor binding，为每项生成现有HTTPS adapter及可直接交给`RelayV2ContinuityAnchor`的timeout/pending options；构造保持惰性，每次实际read/CAS才通过frozen owner-bound provider按exact endpoint/auth mode/credential/trust reference解析本次auth callback与已绑定trust transport，ownerBinding只保留在本地frozen binding中。binder不读取HOME/env/file、不联网、不拥有resolver/backend、retry或第二个timer，也不暴露provider、transport、auth/trust material或mutable config。其上的default-off one-shot opener只在activation时按固定顺序消费caller-owned `trustedHome` 的canonical native open、exact `opened+selfCheck=passed`、`broker-credential.v1` binding、既有genesis与同一Broker Core签发的live-authorization fence，再调用真实`RelayV2BrokerCredentialAuthority.open`；调用前由opener exactly-once回滚store，调用开始即把ownership完整转交authority既有close barrier，失败后不可retry、fallback或改选Agent namespace。opener仍未接production composition/listener/config source，不拥有或暴露store bytes、provider、auth/trust material、ownerBinding、server、overall ready或capability；`authorityContinuityReadiness`仍仅表示该authority的local-state与external-continuity gate，不表示production ready。request fingerprint仍按backend decoded closed tuple字段级比较而非key order/canonical hash。external auth/permission/capacity/rate/timeout code是新冻结的internal namespace，不是Relay wire error；continuity operation timeout 的 5000ms 仍只属于 continuity owner；独立 transport close coordinator 另有 5000ms credential-fence deadline 并已实现 4401/4403/1013 policy，但 显式default-off activated Broker composition已采用该owner，public lifecycle root也已接管caller-supplied `node:https.Server`；但qualified real public TLS/network/device/ready-loss仍须验收。broker credential与Agent extension使用独立namespace/anchorId/ACL/reset/decommission/tombstone history；broker owner binding由future provisioning提供opaque stable identity，不从issuer/tenant credential字段推断。`broker-credential.v1` fatal failure要求credential authority撤ready/admission并全局fence v2 Upgrade/route/active data；未接线的foundation已提供同步owner seam，但production composition仍须注入并采用已实现policy。`agent-transcript-lifecycle.v1` failure复用现有extension store的`AGENT_AUTHORITY_STORE_CONTINUITY_UNAVAILABLE|COMMIT_UNCERTAIN|CORRUPT` mapping并只隔离extension。unavailable保留可证明timeline/cache lineage并repair/reopen authority，不能reset/new epoch；只有corrupt/明确continuity loss允许extension reset/new epoch。基础credential readiness、connection和route/command/terminal保持不变。当前没有qualified production backend与auth/config deployment、provisioning/ACL、真实restart/旧备份/网络/灾备证据、extension isolation/reset wiring或ready-loss真实证据；default-off broker v2 composition存在但`qualifiedRecords=[]`使activation不可达，因此不能ready、enroll或advertise capability。

E0 binder解析出的auth material只在当前attempt内传给HTTPS adapter，不持久化或公开；对成功接管、或能从own data descriptor安全捕获普通discard callable的attempt，exact one-shot owner将其收敛为start或discard，auth header拒绝、auth期间abort及其他pre-start退出都走discard，started attempt不能再次discard。若discard为accessor、Proxy或不可调用而无法安全捕获，则直接fail closed且不读取getter、不执行未知代码。

Node 的显式 default-off Broker shipping root（`src/relay/v2/brokerShippingRoot.ts`）现把 reference-only runtime profile（listen 地址、root issuer/relay URL、trustedHome、TLS key/cert/trust 与 E0 的非敏感 reference、issuer keyring reference）同 deployment 注入的 narrow synchronous privileged resolver（TLS material 与 issuer keyring 的唯一来源）、external continuity attempt provider 及 native state-store loader 装配为现有 production composition，再经既有 public HTTPS lifecycle root 接管新建的 `node:https.Server`：profile/inputs 的 closed own-data validate、genesis/E0 config capture 与 native store→E0→credential authority 的完整 open 全部先于 bind/listen，启动失败 reverse-order rollback，close 先同步 fence public HTTP/Upgrade 与 admin admission，drain 本进程 admin in-flight，再由 lifecycle root drain public HTTP/WSS 并关闭 runtime/store/listener，TLS material 在 server 构造后 exactly-once dispose（dispose 失败只 surface 固定 redacted cleanup 错误）。该 root 的 local privileged admin seam 只在本进程 handle 上暴露，直接调用 credential authority 的 canonical admin mutation owner（issuer/replay rotation 与 host bootstrap issuance）；首个 Host bootstrap secret 只同步交付调用方提供的受限 sink（如 0600 file write），绝不进入 stdout/日志/HTTP，public route 没有 admin 端点，exact-once/replay/失败语义完全复用 authority。`relay-server` CLI 的 production shape 仅由显式 `--v2-profile <path>` 选择，且只经唯一 default-off trusted deployment activation/source owner（`src/relay/v2/brokerShippingDeploymentSource.ts`）取得 deployment inputs：profile 文件只含非敏感 reference（≤16 KiB 严格 JSON，reader 本身 fd-bound owner-read、no-symlink、exact 0600、single-link），TLS key/cert、issuer keyring 与 E0 credential/trust 只来自 trustedHome 下固定 namespace（`.tmux-worktree/relay-v2-broker-deployment/`）按严格 identifier 映射的 fd-bound regular-file/no-symlink、owner、exact 0600/0700、bounded 私有文件（固定 filename/schema，先 eager 校验再 lazy 逐次 secure-read：JSON keyring 与 workload-header 做 closed-schema 校验，TLS PEM、CA 与 mTLS cert/key 只做 owner/0600/nlink=1/bounded secure-read，实际内容由 TLS/E0 消费边界验证；dispose 幂等 zero-fill，错误 redacted），native loader 与 E0 attempt provider 分别复用既有 fixed loader 与 Node provider；任何缺失/unsafe/malformed/unqualified（含 `qualifiedRecords=[]` 的 unqualified native）仍在任何监听或 native mutation 前 fail closed 并返回非敏感 blocker，绝不回退 v1；不带任何显式 v2 lane 的默认 Relay v1 行为完全不变。该 root 不宣告 readiness/capability，不生成 enrollment/credential material，不改变整体 NO-GO。

Broker shipping profile 中 `issuerUrl` 的唯一 canonical path 是 HTTPS root `/`，`relayUrl` 的唯一 canonical path 是 WSS `/client`；credential authority 原样把后者放入 enrollment/redeem response，因而与 Android endpoint validator/transport 同约束。Host shipping profile 是另一条 contract，仍接收 root WSS URL并仅在 Host carrier transport 边界派生 `/host`，不得把两种 profile 互换。

`relay-server --v2-profile` 的同一显式 lane 可再选择 one-shot `--host-bootstrap-output <path>`：shipping root 成功后，进程入口保留该 handle 并直接调用其 canonical `admin.createHostBootstrap`，secret 只经 same-directory 临时文件原子发布为 exact 0600 文件。该 flag 缺省时既有 v2 startup 不变，v1 lane 明确拒绝；文件创建或写入失败先 shutdown 同一 shipping handle，不留下第二 lifecycle/listener，也绝不回退 v1。

Broker 本机开发另有独立、严格显式的 `relay-server --v2-local-dev` activation owner（`src/relay/v2/brokerLocalDevelopmentActivation.ts`）。它不复制 Core、credential authority、HTTP/WSS ingress 或 listener lifecycle，而是构造实际 listener 固定为 `127.0.0.1` 的 reference-only profile；可选 advertised HTTPS origin 只改变下发的 issuer/client endpoint identity。该 owner 把进程内串行 credential store、进程内 monotonic/CAS continuity attempt backend 和进程内随机 issuer keyring 注入上述同一个 shipping root；TLS key/cert 仍从调用方显式指定的 absolute、current-user-owned、regular、single-link、exact 0600 文件经 fd-bound/no-follow/identity recheck 读取，并在 HTTPS server 构造后 zero-fill。CLI 强制提供 `--host-bootstrap-output`，沿用同一 atomic 0600 sink；`--host`、v1 secret 与 `--v2-profile` 互斥。所有 credential/continuity/issuer 状态随进程退出丢失，该 owner 不接 production native loader、E0 resolver、trusted deployment namespace 或 qualification allowlist，不改变/伪造 `qualifiedRecords`，也没有恢复、HA、fallback、qualification/readiness/capability 或 production GO 含义。默认 v1 与 `--v2-profile` 的 production fail-closed 路径保持不变。

该本机 owner 要求显式非零端口，实际 bind 始终固定为 `127.0.0.1`。缺省 identity 仍是同 authority 的 `https://localhost:<port>/` issuer 与 `wss://localhost:<port>/client` relay；显式 `--v2-dev-advertised-origin` 只接受严格 HTTPS root origin，并把其 root 作为 issuer、同 host/port 的 WSS `/client` 作为 client relay，不会生成 `:0` enrollment。外部 TLS/TCP proxy 归 smoke deployment；Android 模拟器仍可用 `adb reverse tcp:<port> tcp:<port>` 复用缺省 localhost identity，Host 继续从对应 root URL 派生其独立 `/host`。各实际 TLS 边界仍须验证可信证书链与对应 hostname；该配置不构成 qualification 或 GO。

Broker 另有显式 default-off 的 `relay-server --v2-single-node-self-hosted` 非 production deployment policy（`non-production-single-node-co-located-sqlite-v1`），唯一 activation owner 位于 `src/relay/v2/brokerSingleNodeSelfHostedActivation.ts`。它只接受 Linux x64、Node >=22.16 与 ext-family filesystem，要求调用方显式给出 listen host/非零 port、独立严格 HTTPS advertised root、owner-only TLS key/cert 和现存 canonical exact-0700 dedicated state directory；因此默认路径不会意外公开监听，而显式 `0.0.0.0` 可服务内网手机。该 owner 以 state directory descriptor、current uid、machine-id digest、directory device/inode 及固定数据库名约束一个 exact-0600 SQLite，配置 `synchronous=FULL`、`locking_mode=EXCLUSIVE`、strict schema 与单进程排他启动；数据库内同一 owner 事务性保存 credential opaque bytes、monotonic continuity row 和稳定 issuer keyring/kid。continuity CAS 只接受 sequence 0 genesis 或 exact `current.sequence+1` 且 parent 指向 current commit 的后继。它只通过窄 store/continuity/keyring ports 调用 canonical `RelayV2BrokerCredentialAuthority`，并继续复用唯一 Broker Core、shipping root、HTTPS/WSS lifecycle 与 admin authority，不复制 credential schema/authority。

该 self-hosted CLI 另有严格隐藏、只与 `--host-bootstrap-output` 配对的 `--v2-self-hosted-bootstrap-correlation <attempt-id>` seam。attempt-id 是 1–128 UTF-8 bytes 的非敏感 Relay identifier；CLI 不把它解释为 filename、Host target 或 credential state。shipping admin 的 `publishHostBootstrap({ correlation }, sink)` 在 canonical credential authority 内把 immutable anchor/issuer/relay profile、Broker-generated bootstrap selector、TTL 与 correlation 做 exact fingerprint；首次 claim 在同一个 credential-state/continuity commit 中同时写入 bootstrap verifier 和 `host.bootstrap.publish` encrypted response-loss replay。commit 后、sink 前 crash 的 fresh process只会解密并返回同一个仍有效 token；sink 同步完成原子 0600 publish 后，只有同 authority 签发的 process-local opaque receipt能把该 replay改为 acknowledged tombstone。sink failure不ack；ack commit不确定会withdraw/close，restart按durable state决定resume或already-acknowledged，绝不 remint。pending token过期、correlation/TTL/profile mismatch、ciphertext/selector/receipt不一致均fail closed。record与关联 bootstrap最多保留到bootstrap expiry加既有10分钟replay retention，继续受现有2048 bootstrap/4096 replay容量约束；ack retry不再调用sink，retention后operator不得复用attempt-id。未提供该 hidden seam 时，现有 `admin.createHostBootstrap` one-shot行为不变；production profile、local-dev、v1和默认shipping均不采用它。

该 self-hosted profile 是 shipping root 唯一允许的 non-production alternate shape；它与 production profile 的 E0/native inputs 严格互斥。co-located continuity 明确不是 E0，不能满足 rollback-independent continuity 或 G3；目录复制因 machine/directory identity 不匹配而 fail closed，旧快照恢复、同目录数据库替换、state import/override、backup restore、HA/failover/DR 均被 deployment policy 禁止且没有实现入口。进程重启使用同一目录、endpoint 和 TLS identity 时，credential state、continuity 与 issuer keyring/kid 一并恢复，因此既有 Host/Android credential 可继续验证。CLI 在 activation 前安装一次性 SIGINT/SIGTERM/SIGHUP latch；signal 会 fence 后续 bootstrap，已发布 handle 只经其 `shutdown()` exactly-once drain 后退出。该 lane 不读取/修改/伪造 production `qualifiedRecords`，不创建 E0 evidence，不放宽冻结 required capabilities，不回退 v1，也不改变默认 v1、production `--v2-profile` fail-closed 或整体 NO-GO。

Android 生产源码还包含可选 `agent.transcript-lifecycle.v1` extension 的独立 lifecycle/notification pure reducer foundation，并由 JVM suite直接消费共享 client machine fixture；fixture中的 `command_status` 由测试 base-owner composition stub处理，所以这只构成 reducer fixture conformance，不构成 production consumer machine conformance。它只建模有界 lineage/cursor/checkpoint、run/turn lifecycle、notification disposition与非消费型 system-intent preflight；command ledger仍属于基础 v2 owner，不在该 reducer 内复制。Android 已在隔离 owner 内接通 strict public artifact、typed durable operations、row-oriented Room v5 materialization、durable notification claim 与 revision-pinned read projection，并新增 default-off runtime composition seam：同一个 durable repository 实例装配现有 runtime consumer、notification dispatch coordinator和read projection；上层必须逐个显式调用 `handle(effect)`，composition不订阅actor effects，非Agent effect与Agent unavailable effect都以携带完整原effect的`NotOwned`交给upper-layer dispatcher；只有携带exact failed request/admission identity的unavailable effect才归RequestSync redrive owner。`dormant(...)`与base composition的窄notification platform port默认仍是`NOTIFICATIONS_DISABLED` closed suppressor；production `AppContainer`以`applicationContext` lazy构造并注入已有`AndroidAgentTranscriptLifecycleNotificationPlatform`单例。构造只取得Android system service并连接port，不启动database、network或actor，不请求通知权限、不调用`notify`，也不执行notification intent。

H2 materialized-state与持久pinned `state.snapshot` spool foundation现由canonical default-off Host composition采用 与持久 pinned `state.snapshot` spool foundation：注入 discovery 后在 H0 内维护 aggregate/scope coverage、以 `(hostEpoch, scopeId, backendKind, backendInstanceKey)` 区分的 opaque scope/Session mapping、lineage used-ID marker、per-dimension revision 和全局 eventSeq；spool 只从同一 H0 critical-section cut 投影 records，并独立拥有跨进程 owner fence、immutable chunk/manifest、quota reservation、lease、release tombstone 与 fail-closed recovery。这些内部安全不变量不代表完整 rebuild/non-reuse runtime 已交付。已建立 authority 的 SSH scope 不可达时只更新 reachability，并保留 last-known complete Session cut。H1 create 通过窄 owner seam 在 canonical ledger transaction 内预留/消费容量，真实 backend incarnation 只能来自 executor outcome；welcome capture、subscriber register 与严格同步 event enqueue 共享 H0 serializer。完整 root 在原子替换前受同一 H0 serialized-byte hard budget 约束，capacity、持续 generation conflict 或 materialized authority 矛盾会在 H0 锁存 readiness fence，只有稳定且预算合格的同-lineage reconcile 才原子清除。普通 discovery absence 不会结算不确定 reservation，已有 backend positive evidence 的 reservation 也不会进入 negative candidate；只有可选的 canonical executor fenced-negative adapter 能在同一 cut 释放无 positive evidence 的 reservation，释放结论以 host-lineage negative tombstone 保留，迟到或未知 positive correlation 会 fail closed。`stateSnapshotSpool`仍是durable recovery与H2签发的唯一owner：只有本次`open()`恢复、未过期、当前owner持有且manifest与fresh materialized candidate在hostEpoch、event/scopes cut、record count、canonical bytes和digest上exact一致的complete cut才可签发opaque recovery candidate；按`(snapshotCreatedAtMs desc, snapshotId asc)`确定性选择，不匹配时不扫描、重建或fallback。candidate私有绑定当前H0 process的hostInstanceId、owner fence/spool generation、snapshot/materialized cut identity、同一spool内部receipt，以及签发时创建的exact composition pair；issuer half只在canonical spool registry，receiver closure与readiness sink只在canonical host composition。composition异步完成receipt verify、subscriber activation lease与readiness发布后才返回完整runtime；公开H2 lifecycle只有冻结`close()`。cut release、owner takeover、spool/fatal close、sink close或composition dispose会同步撤回 H2；expiry没有composition timer，只有spool owner在后续cleanup或其他owner operation中观察并prune后撤回。spool 另有一次性 fresh-install bootstrap port：fresh install 没有任何 recovered pinned cut，该 port 由同一 materialized owner 的 `reconcile()` 捕获 fresh cut-source candidate，复用同一 private one-shot canonical pair receiver 完成 H2 activation 并交出同一 runtime H2 与空 spool；spool closed/fatal/quota 超限/recovery 不完整、已签发过 bootstrap candidate、或持有任何 active cut/reservation/tombstone/in-flight build 时只返回 null，hostId/hostEpoch/hostInstanceId 在 metadata 序列化边界复核，`release()` 只释放该 cut-source candidate/activation lease。canonical composition 先查询该 port、miss 时回退既有 recovered-candidate 路径，两者结构性互斥；首个真实 client snapshot 仍走既有 reservation/buildAndPublish。该 seam 已进入 canonical composition 的 H2 readiness/claim chain；仍未接的是 CLI/public production activation、真实 discovery 与 Android/TLS 互操作，不宣告 production capability，也不生成 credential/enrollment。当前默认 CLI/shipping 产品运行路径仍是 Relay v1；显式default-off v2 roots不因此成为shipping或capability。

上述 H2 组合 authority 不由 host 字符串或普通 object 拼接。materialized-state foundation不再公开可反射capture的runtime/cut bundle；它持有exact owner并通过闭合`openStateSnapshotSpool()`把同一owner交给canonical spool，普通caller只能单独使用不具备H2签发权的standalone cut-source spool。candidate本身是frozen、空外形、one-shot token，不含receiver、activator、sink、runtime/spool、receipt或release accessor。唯一candidate registry与closed runtime-composition operation位于canonical `stateSnapshotSpool.js` entry；签发时另建的pair只把issuer half留在该registry，receiver closure由canonical host composition的私有WeakMap持有，readiness sink也由composition内部捕获。foreign/new pair、copy、Proxy、replay、identity mismatch或spool A + H2 B都不能claim既有candidate；只有exact receiver完成verify、activation lease与readiness后才返回完整composition，绝不向普通caller交付可拆分H2/spool ports。构造失败会exactly-once释放activation并撤candidate/receipt/issued状态。公开composition仍不含receipt或`activate(issue)`，H2 lifecycle只有`close()`。任一activation release同步异常、非法返回或native Promise rejection都会撤H2且不重试；hostile thenable不被读取或assimilate。

相邻的 `materializedReconcileLifecycleOwner` 是独立、default-off、未接线的连续 discovery/reconcile 调度 owner：它只把注入的 materialized foundation `reconcile` 作为窄 method port 驱动，构造无副作用；`start()` 立即触发一次扫描，随后由每次扫描结束后才重臂的周期 timer 与显式 signal 触发。同一 owner 内扫描永不重叠；扫描或 reconfigure barrier 期间的触发最多合并为一个后继扫描，同批调用共享该后继的单一 outcome promise，不保存 per-caller 队列。`reconfigure()` 严格校验闭集 own-data 配置（拒 accessor/symbol/多余键/Proxy）并同步 snapshot，先完全排空在飞的旧 generation 扫描再执行注入的 discovery/config swap，之后只发出一次新 generation 扫描，旧配置的迟到结果不能写入新配置 materialized state；swap 失败在 fail-closed 后继扫描已调度后拒绝。`close()` 停 timer、拒绝新触发并排空已开始扫描，之后没有任何 callback 再调度或写状态。partial/unreachable/失败的 fail-closed 语义仍完全由 materialized foundation 既有契约处理；该 owner 不新增 retry/backoff/cache/状态机，不涉及 endpoint/credential，不产生 capability，也没有 CLI 或 production composition callsite。

可选X extension现有可由canonical Host显式附加、但默认关闭的Node host authority reducer、public codec、durable store 与 replay runtime foundations。reducer 完整直接消费 `agent-transcript-lifecycle/v1` accepted authority machine fixture，以显式 restore/corruption boundary、不可变持久索引、active-turn index和全状态 count/canonical-byte预算处理受控 source event。store 把 reducer snapshot、source cursor/dedupe、公开 event log、pinned snapshot/replay cut与 host commit-time retention组合为单一 optimistic transaction candidate，再在既有跨进程锁内以 expected-current CAS原子发布；最终落盘 bytes 会用读取侧相同的 strict key/node/byte inspection 和完整 restore 校验，pinned page 则在 durable cut 前逐页通过真实 public codec 的 256-item、1 MiB 和 JSON limits。它要求 composition显式注入共享 Relay v2 monotonic/CAS continuity port与 owner-bound `anchorId`，在本地 exact durable-byte checkpoint发布后提交外部 anchor再 ACK；缺失、超时、冲突、回滚或 reconcile-required均 fail closed。外部 checkpoint可恢复单一 state-before-anchor窗口并拒绝 paired state+witness rollback，本地 witness不再被当作 rollback authority。Node同目录另有固定Codex `0.144.5` schema 2的producer、bounded LF notification source、injectable process-controller authority、default-off trusted-source composition与one-shot activation foundations；notification source独占一个future controller提供的byte channel，只负责有界切帧、单一sink Promise顺序、admission withdrawal、source cancel和drain，不认证、不spawn也不解析Codex事件。进程内receipt primitive只允许future controller所持exact issuer向一个composition独占receiver签发one-shot opaque lease，普通结构、copy、Proxy、replay与foreign issuer/receiver都不能伪造，但该primitive本身不认证app-server进程。activation只接受controller、既有runtime与canonical resolver，内部独占issuer/receiver/authority/composition/lease并按controller claim、bounded source claim、lease issue、composition独立H2校验、attach的固定顺序完成一次handoff；构造不claim process/source且不调用H2。其close在controller pending、lease issued、H2 pending、attach与enabled阶段永久latch，callback-phase continuation避免controller、sink/ingress与raw cancel/closeAndDrain内部await形成self-wait，外部barrier仍等待late source、完整source drain和producer/ingress FIFO。handoff前只有authority cancel，handoff后只有composition cancel；built activation与direct controller/source entries共享不可逆进程registry。composition仍内部生成不可由caller控制的`sourceEpoch`，H2只提供当前`evidence_only`映射而不认证app-server进程。仓库仍没有 production monotonic authority adapter、真实process controller contract/implementation、production app-server ingress或external continuity backend；它仍未接`relayHost run()`、真实trusted process controller、broker route/capability advertisement或qualified external continuity。G4与 capability advertisement仍为 false，当前产品没有远端 Agent reply/lifecycle authority。

Android 还提供独立的 `RelayV2ConnectionActor` seam：它复用 v2 profile/credential 与 production codec，以串行 socket ownership、有界 action/effect queue、双代际 fence、握手 watchdog 和 hello query/resync/reject effect 固化客户端 runtime 边界。`AppContainer` 现复用唯一 `RelayV2StateDatabase` 装配 Agent durable repository和真实Android notification platform owner，由 `RelayV2BaseRuntimeComposition` 的唯一 effect pump 以同一 actor apply/handoff fence 委托 dormant runtime composition；同一 base composition 另以现有 selection/read/presentation controllers 把 exact opaque Session cut 的单页 transcript projection 接到 `V2ViewModel`。optional capability advertisement仍为空，production在actor capability/read cut处即返回`Unavailable`，零Agent Room read且零platform call；只有exact client/broker/Host三方协商的Agent effect在actor apply lease内完成durable commit与notification claim后，才会把content-free ticket最多一次交给同一注入platform，suppressed/failed均不建立retry。真实platform owner已装配不等于真实Agent capability/runtime或通知产品能力已交付，因此仍不能宣称Agent reply、Waiting/Failed/Completed或通知可用。独立的 version 6 `RelayV2StateDatabase`/repository 使用另一数据库；其中基础 state-sync authority 已被 base composition 复用：六张基础 v2 state-sync 表按 `profileId+principalId+clientInstanceId+hostId+hostEpoch` 持有 authority cursor/required watermark、Scope、opaque Session、pinned snapshot staging records及有界 durable state-event buffer；另有 activation-scoped Outbox/terminal、Agent lifecycle/notification rows；version 5 加入的完整九列 Agent timeline namespace row-oriented materialized entry、snapshot staging/records与 snapshot/gap LIVE buffer继续保留；version 6 新增terminal post-commit batches/fences/meta journal。5→6只创建相应表与索引，不解析、回填或改写旧rows。完整 state cut 通过 count/canonical digest后才在单一 Room transaction替换 cache并顺序合并连续 event；exact disconnect receipt 在同一 profile cleanup transaction 清理该 profile 的所有 activation/timeline rows。它不迁移 v1 `hostId+name` 行，也不保存 credential。

Android 的纯 Relay v2 Outbox authority core 与 Room 持久 owner 以完整 profile/principal/host lineage、dedupe window、command、operation、opaque Scope/Session 和 canonical request fingerprint 为身份，确定性处理发送、接受、查询确认、结构化 `not_accepted` 重试/换号、host epoch 变化、late final、lane 阻塞与硬容量失败。production base composition 已接通 query/recovered dispatch 与 bounded fresh `QUEUED` durable producer：同 lineage activation admission 只把 `SENDING`/`ACCEPTED`/`CONFIRMING`/`AMBIGUOUS` 送入 query，`QUEUED` 留给 fresh producer；actor 返回 exact `OnlineReady` 后先按 durable commit 顺序 flush recovered capability，再由 core 在同一 Room transaction 按 creation order 选择至多 32 条 DispatchEligible 并提交 `QUEUED→SENDING`，commit 后才签发 Execute capability，唯一 transport gate 仍是 actor `sendIfCurrent`。commit 后发送前或 partial send 后失败都保留 `SENDING`，重启只 query、不盲发；没有 StateFlow 观察、第二 sender、内存权威或自动 command 重发/重签发。受限 Session reply UI/enqueue、已 admission 的 existing-Session kill，以及每个 materialized 可见 Scope（包括空 Scope）的 durable `create_worktree` / `create_terminal` 已按 actor ONLINE lease 与 Room opaque cut 接入；额外 extension command UI 仍未接，这个切片不宣告完整 APK command runtime、ready 或 capability。

Android admitted base runtime现已装配既有纯Kotlin Relay v2 terminal checkpoint/reducer foundation。版本化 stable timeline identity 完整绑定 profile activation generation、principal/client、host authority/process、Scope/Session、stream/generation，以及非敏感 resume-token credential reference/fingerprint；`openId`/`closeId` 则分别属于可持久的单次 logical control attempt，不会被提升为 stable timeline identity。首次 network send 前，独立 pre-open checkpoint 先持久化本地可信 target、mode、open attempt/fingerprint、delivery authority与 mode 所需的 frozen generation/nextOffset/resume credential evidence；每次 network dispatch 都必须签发未被该 logical attempt 使用过的新 requestId，持久的有界 request history 隔离迟到响应，同时 logical fingerprint 保持不变。RESET result 必须同时轮换 frozen resume generation 与 token fingerprint。已应用的 logical open 另保留精确 result lineage，以便同 attempt 的 response-loss retry/deduplicated replay 幂等收敛而不重新清 parser 或 control；deduplicated NEW/RESET 可以携带首次 response 丢失后已增长的非零 tail，并从冻结 baseline 按 ring replay。pre-open correlated reset 持久化被消费 attempt 的精确 open fence。delivery fence 复用 actor-owned `RelayV2EffectGeneration(profileId, profileGeneration, connectionGeneration)`，再加同 activation 内可恢复的 authority generation 与 Android 本地 callback-dispatch token；进程重建可通过更高 authority generation 安全重置 actor counters，同 authority 内再比较 connection/local counters，profile activation 变化不能原地 rebind。它不接收、伪造或持久化 broker carrier `routeId`/`routeFence`，旧 authority 或旧本地 token 的 callback 永久无权；Rebind/RESUMED 会用新 fence 确定性重驱同 generation pending input/resize。raw-byte network receipt 只推进独立的 received watermark，只有绑定完整 identity、当前 open attempt、delivery authority、parser continuity 和精确 byte range 的 parser callback 确认连续应用后，才推进持久 parser-applied offset并产生累计 ACK；restore 必须先以持久 operation 的原始完整 callback token 验证 proof，再允许换绑 delivery，不能先把旧 operation 改写成新 fence。replay 已收至 captured tail 时，live output 仍可在硬上限内继续排队，由 parser 按序跨越 replay boundary。restore 会先按 O(1) queue count 与有界 raw-byte/shape preflight 拒绝不可信状态，再做深 snapshot；若持久 checkpoint 含 parser write/reset in-flight operation，adapter 还必须提供匹配完整 callback token、applied offset 与状态的精确 proof，core 才会完成 callback 或安全重驱，否则进入显式 reset。该 core 独立持有有界 parser pending queue、input/resize pending 与累计 ACK、generation/closed/finalize 时未 ACK input 的 `AMBIGUOUS` 记录、replay、不可变 closed tombstone、可单调退化的 retained-ring metadata、可跨重连精确查询的 close intent 和 reset 原因；每个 queued input/resize effect 还带可撤销的 control-dispatch lease，未来 adapter 必须在真正 socket write 前向 pure core 校验 exact pending payload 与当前 lease，closed、close intent 或 reset 会原子撤销该 lease。closed 到达即禁止新 input/resize/close backend effect，旧 generation 或 closed 时 pending resize 会直接丢弃，known-final open/replay 必须同时满足 exact tail 和 retained-ring coverage。单帧上限为 64 KiB，terminal→parser pending/credit 上限为 512 KiB，并对 checkpoint、network request history、input、resize 和 frame count 设硬上限；损坏、过期或超限 restore 返回绑定当前本地 authority 的最小 reset 结果，不复制或保留不可信 queue。pure core仍不直接依赖Room、WebView、xterm、actor、socket或Compose；`RelayV2TerminalProductionComposition`经Room checkpoint/journal、exact actor send、raw-byte parser ACK与Compose attachment闭合这些ports。parser continuity、完整identity或schema无法证明时仍fail closed为`RESET_REQUIRED`；该client wiring不表示端到端terminal capability或真实设备互操作已交付。

repository 消费代际 effect 时必须在 actor-owned apply lease 内完成整个持久化事务；公开 state 的 `matchesGeneration` 只用于非原子的代际预筛，不拥有 phase authority，也不能充当 profile-switch barrier。disconnect/profile switch 会先原子禁止旧代际取得新 lease，再等待已进入的旧事务完成，receipt 之后才允许通过显式 API 清理旧 profile 的 v2 state。Android production profile runtime 现已注入 strict OkHttp HTTPS credential exchange：redeem/refresh 只使用 strict codec 的精确 endpoint/body，并将每次调用封闭为单次 HTTP/1.1 exchange；cold-start admission 保持零网络。confirmed-enrollment 只有在显式、default-off review 中用户确认后才可触达 exchange；QR/Intent 只填充公开的非敏感 review state，enrollment code 仅由内存 session owner 私有持有，current-active-profile refresh 仅由显式 Connect/Retry action 触达：仅当 admitted profile 的 access credential 已过期（或存在 durable pending refresh attempt）且 refresh credential 本地仍有效时，同一用户动作经现有 refresh owner refresh、重跑 closed startup admission 并重建 connect；probe 只在无 live composition（缺失、终态失败或从未连接）时运行，cold-start admission 与后台 reconnect 保持零网络，refresh 失败保留 typed 错误且不回退 v1。enrollment/refresh不自动连接；`auth.expiring`已接exact rollover。本地 admitted-v2 用户驱动的 self-revoke quarantine/confirmed cleanup saga 已闭合但仍default-off；没有真实TLS/device/qualification/signing/release证据，因而不构成production availability或GO，也不宣告capability/readiness。bounded RFC6455 WSS adapter 已作为 base composition 的唯一 socket 入口，使用系统 trust、RFC2818 hostname verification 和单次 HTTP/1.1 Upgrade，在 frame reader 分配或读取 payload 前执行 1 MiB frame/fragment 累计上限，且不 offer 或接受 WebSocket extension。actor/profile closed startup admission、Room 基础 state-sync repository 与 bounded WSS 已接入 `AppContainer`、`V2ViewModel` 和 base composition root，但没有真实设备或公网 Relay TLS 互操作证据。连接前 exact activation Outbox 允许同 lineage bounded `QUEUED` 留给 fresh producer，queryable attempted state 仍进入 query；foreign/corrupt/overlimit 与 command/terminal/Agent/其他未拥有 effect 会关闭且不 ACK、丢弃或 fallback。除上述显式 enrollment review/confirm 与本地self-revoke saga外，refresh 产品 UI、真实 target resolver、canonical executor adapter、H2 runtime adapter、production carrier pump、四个冻结 operation 之外的 extension Outbox UI/enqueue与capability advertisement仍未交付；terminal的Room/journal、actor、WebView parser与Compose接线已进入admitted base runtime，但没有端到端Host capability或真实设备证据。Dashboard及broker/host默认CLI与shipping数据流仍是Relay v1；显式v2 compositions保持default-off，server/host 仍未达到 production Relay v2 readiness，端到端 Relay v2 继续 NO-GO，因此 Android 的 base-sync 连接状态不能提升为 ready。

Android admitted-v2 的正常用户恢复入口仍由上述 owner 闭包承担：`Retry now` 在 base composition 的 `connectionLock` 内取消已拥有的 backoff 并直接 claim fresh attempt；terminal failure 返回 typed 结果，由 `V2ViewModel` drain/close exact 旧 composition、清同一 UI slot/cuts，并只从当前重新 admission 的同 profile 安装 successor，失败不回退 v1。`Refresh sessions` 与 create 页 target retry 在 v2 admission 下进入 actor 单一有界 manual-resync port；该 port 绑定 exact profile/generation/current transport，复用 `hosts.snapshot.get`、full state snapshot、既有 recovery adapter 和 Room commit，不建立 UI→Room 写旁路。这里的 refresh 是资源 state refresh；credential refresh 仍只由显式 Connect/Retry 的既有 credential owner 触达，Relay v1 refresh 行为不变。

Node 侧还包含只依赖注入接口的 process-scoped H3 terminal core：它独立拥有 v2 generation、raw-byte ring、connector/route/generation fence、credit、input/resize dedupe 和 control tombstone，不复用 Relay v1 stream map。sessionId 到 backend/control target 仍必须由未来 H2 canonical resolver adapter 提供。未接线的 H0 durable terminal lineage authority 已把跨进程 open lineage、stream authority 和 close intent/tombstone 收敛到一个 closed/versioned/bounded HostState record；全部变更通过单一同步 transaction 权威化，并以 claim token/fence、durable monotonic generation high-water 及精确 target/pane/resumeToken hash binding 处理 claim→complete/fail、natural close、finalize 和 release。它不保存明文 resume token；pending exact retry只持久结算并重放 `stream_lost`，commit-uncertain 在同一 serializer cut 读回 reconcile，reset retirement也只接受精确旧 binding。terminal backend/ring 仍是 process-scoped：hostInstanceId 改变会把旧 executable authority 原子退休成有界 lost evidence并释放 live quota，绝不把旧 generation 转移给新进程；pending close intent 只通过独立 owner fence 迁移执行权，immutable close winner 保持不变。new/reset generation 由 durable claim transaction 签发，在 complete CAS 胜出前只保留为不可见 provisional record。`terminal.open`/resume/rebind 始终只是 observer，cols/rows 只作为 attachment-local 显示提示，首次 input/resize 才通过显式注入的 terminal-control adapter 懒申请完整 owner/lease/fence single-writer 权限；该 adapter 只把 H3 typed port 翻译为现有 `ownership.status`、`lease.*`、`input.raw` 和 `input.resize` request，原始 bytes 使用 canonical Base64，不保存 lease/operation 事实、不自动启动 daemon、不重试 input/resize，也没有 direct tmux fallback。connector/route/fence 改绑会先释放并清空旧 route 控制窗口，释放结果不确定时只允许 observer 输出并把控制保持为 IN_DOUBT。`src/relayHost.ts` 的 default-off canonical production opener 已把恢复后的 durable lineage authority、terminal-control adapter 与 H3 core 装配到同一 owner，并在 facade 发布前等待 H3 activation；capability gate 已随 readiness → pre-carrier claim → host.hello 链 default-off 闭合，仍缺失的是 CLI/public production activation 与真实端到端证据；Android client接线已闭合，但没有 production-activated Host capability、真实设备或端到端证据。durable open record 经恢复/重放进入 `stream_lost` reset 时，disposition=`new` 与 `resumed` 现都把 durable record 保存的 `replayFromOffset` 携带为 reset 的 `requestedOffset`，只有 disposition=`reset` 保持 null，修正了 mode=new 重试丢失 durable offset correlation 的缺陷；该修正只在既有 durable-record 恢复逻辑内，不新增 owner、port 或 wire 字段。terminal byte plane 仍接受 caller 注入的 `RelayV2TerminalByteBackend` port；default-off canonical composition 在未注入时默认装配 exact-observation byte plane（open 只把 admitted claim 原子消费为只读 observation 并从其 outputCursor tail，首个 input 经 lazy lease 在同一 compound channel 重 prepare→fence→consume，release 后回 observing 继续 tail，cleanup 经可重试 barrier 且失败由 adapter.close() 重 drain 并上抛）；这只是 host-side internal composition 事实，wire/ABI 不变，仓库仍没有 Relay byte-plane atomic byte observation owner/ABI、CLI/public production activation 或真实端到端证据。因此这些仍不表示 H3、G2 或任何 capability 已完成。

Node 侧另有未接生产入口的 `RelayV2HostRuntime` route composition core。它实现 `RelayV2HostCarrierRouteSink` 后的唯一 v2 public-frame dispatch 与 exact-binding fence，重新经过 strict codec 后只把 command、materialized state/snapshot 和 terminal frame分别送入 H1、H0/H2、pinned snapshot spool 与 H3 typed port；actual-authority port factory只做方法适配。`hostRuntimeComposition.ts`在其上提供default-off装配：复用现有六源readiness owner、actual ports和runtime，只暴露host-side route sink与typed readiness lifecycle，不创建carrier或重连。Composition不接受raw H1 authority：`openRecoveredAuthority`只在最终`store.serialize`审计cut内签发opaque、空外形、one-shot candidate，并exact绑定command plane、source generation、hostId以及该snapshot的hostEpoch/hostInstanceId；独立H1 activation通过canonical `hostCommandPlane.js` ESM entry消费candidate，在触碰readiness sink前比对composition三元identity，随后同步发布H1并把execute/query/issueDedupeWindow gated facade私有交给runtime。公开`readiness.h1`只有冻结的`close()`。H2输入同样不再是可单独传入的snapshot authority，而是canonical spool为exact recovered cut签发的`h2RecoveryCandidate`。composition factory与carrier composition均为async open；canonical spool entry先用签发时pair做不消费preflight，只有canonical composition持有的exact receiver closure才能claim candidate并取得其内部readiness sink，随后在同一closed flow内完成identity、receipt verify、activation lease和readiness；全部成功后才构造并返回完整runtime composition，普通caller无法取得中间runtime H2或spool ports。公开`readiness.h2`也只有冻结的`close()`，没有`activate(issue)`；copy、Proxy、replay、foreign-entry/foreign-pair、identity mismatch或split issuer均不能推进H2。任一构造失败会清理已创建的H0/H1/H2/H3 owner，并撤销已claim的H2 candidate/receipt/activation。H1/H2/H3 close、fatal或composition dispose会先同步撤readiness、触发现有4406 route fence并拒绝新调用；dispose执行全部H0/H1/H2/H3/sink/runtime cleanup，并等待已admit H1调用排空与H3 manager shutdown。底层runtime在任一readiness同步撤回时先fence现有route、unsubscribe H2 subscriber、detach H3 route并以4406关闭，且旧connector generation不可恢复。每条route的未完成request、pending operation与outbound frame/byte都有硬上限，transport已接管但尚未receipt的frame/byte仍计入额度，receipt超时以1013 fail closed。hello先从私有recovered H1 facade取得同lineage command window，再进入H2 `linearizeWelcome`的同步builder/W+1 barrier。该core、activation owners与composition都不实现ledger、resource、snapshot spool、terminal generation、credential、socket reconnect或canonical executor。`src/relayHost.ts` 的唯一 default-off canonical production opener 用已恢复owner构造composition，并在发布facade前依次激活H0/H3；它可选唯一拥有同lineage的protocol-v2 Dashboard management session，省略配置时facade不变。CLI `run()` 与 public production activation 仍未接线；actor 构造时的静态 `advertisedCapabilities=[]` 只是禁止绕过 readiness → one-shot pre-carrier offer claim → carrier connect consume → host.hello 能力链的 fail-closed 输入，不代表最终 advertisement 恒为空。这不构成G2完成，当前默认 CLI/shipping 路径与其已广告capability仍只有Relay v1；显式default-off v2 root本身不构成shipping或capability。

## 运行数据流

### Dashboard：本机

```text
React
  -> DashboardBackend
  -> Tauri command
  -> Rust feature
       |-- read/catalog/PTY/git/files -> local tmux/git/filesystem
       `-- managed create/restore/kill
             -> bundled same-version cli.cjs + Node.js
             -> tw rpc v1
             -> git worktree + tmux + managed state
```

本机 managed mutation 优先使用 `.app` 内 bundled CLI。只有 bundled runtime 不可用时，才允许使用版本与完整 RPC capability 都和 Dashboard 匹配的全局 `tw`。两者都不可用就明确失败；不会进入 direct Rust/tmux creator。

catalog 是发现视图：Rust 将 live tmux 状态、managed state 和严格的 worktree/terminal identity 组合后返回。读取可以容忍某一路暂时不可用，写入则遵守更严格的权威边界。

### Dashboard：SSH Host

```text
React -> Tauri/Rust -> SSH Host
                       |-- tw rpc list / capabilities
                       |-- tmux/git/files read paths
                       `-- tw rpc mutation -> remote tmux/git/state
```

- 已连接 Host 只来自本机 `~/.tmux-worktree.json` 的显式 `hosts`；`~/.ssh/config` 只提供添加候选。
- 远端 catalog 合并 `tw rpc list` 的 managed 条目和严格匹配的 legacy tmux/worktree 发现，按原始 tmux name 去重；RPC 条目优先。
- 远端 create-worktree、create-terminal 和 restore 必须经过兼容的远端 `tw rpc`；缺失或 capability 不兼容时要求安装/升级，不回退到 Rust 手写 SSH git/tmux mutation。managed kill 先走 RPC，只有下文“Managed lifecycle 与兼容层”第 4 项枚举的 legacy 信号才允许 direct tmux compatibility path。
- New worktree 对话框按所选 Host 扫描 orphan，并通过 DashboardBackend 探测该目标上的受支持 Agent；创建与 restore 只能从探测结果中选择 `available` 的固定命令，不接受任意文本，探测失败或无可用 Agent 时不允许提交。远端扫描和删除只接受该 Host 配置的 `worktreeBase` 下 `project/worktree` 两级真实 Git worktree；删除由 Dashboard 的 SSH/Git adapter 执行，保留分支，并在用户明确强制删除时于提供 `/proc` 的 Linux Host 上终止 cwd 仍位于目标目录内的残留进程。远端 restore 仍委托目标主机的 `tw rpc restore-worktree`。
- Dashboard 可以把 `.app` 中的 CLI 复制到 Host 并安装 wrapper；远端仍需要 Node.js、git 和 tmux。
- PTY、文件和 git 读取按 Host 路由通过 SSH 执行，不经过 Relay broker。

远端 managed terminal 的 SSH tmux attachment 保持 read-only，Dashboard 输入统一经过目标主机的 terminal-control authority。xterm 产生的 mouse/focus transport report 不得作为 `input.raw` 粘入 pane；受控 attachment 的滚轮使用显式、受 fence 保护的 `input.scroll`，不伪装成 raw mouse report。backend 对普通 pane 进入 tmux copy-mode；只有 pane 已进入 alternate screen 且应用明确声明 `mouse_any + mouse_sgr` 时，才在同一次 `input.scroll` mutation 内合成有界 SGR wheel 输入，让 Claude Code 一类不产生 tmux scrollback 的全屏 TUI 使用自身滚动逻辑。该分支不放宽 `input.raw` 或 attachment transport report。authority 进入 `RECOVERY_REQUIRED` 时，Dashboard 只有在用户明确确认后才发送 host-aware `handoff.force`，接受上一次操作可能已生效并推进 fence，不重放不确定输入。显式 recovery 只对已持久化的 recovery target 开放，并从该 target 的稳定 identity 确定性派生唯一 planned output generation；tmux capture mutation 与 authority commit 之间中断时，后续确认只续接这个 exact generation，任意其他活跃 generation 继续 fail closed。

远端 UI identity 使用 `<hostId>:<rawTmuxName>`；实际远端命令始终使用 `rawTmuxName`，不能把展示用 composite key 直接传给 tmux。

同一 B7e shared owner 现另有 default-off Native Host WSS adapter/runtime foundation，只消费已升级且精确协商 `tw-relay.host.v2` 的 socket，并复用唯一 Core、producer registry、transport-close、expiry 与 client-effect owner；它不创建 listener、WebSocketServer、端口或 HTTP 101，也未接 production server/E0/capability。

### Android：Relay v1

```text
Android RelayV1ConnectionActor
          |
          | WSS /client + shared Relay secret
          v
relay-server (broker: auth + routing)
          |
          | WSS /host
          v
relay-host on the Mac admin machine
          |-- observation -> tw serve / local or SSH terminal stream
          `-- input       -> target terminal-control -> fenced backend write
```

Mac connector 是业务桥接点：它读取和 Dashboard 相同的项目、Host、terminal metadata 与 managed state，并用 Mac 的 SSH 权限聚合 local 和 remote scope。broker 只鉴权、维护 Host/Client/stream 路由和转发 frame；它不拥有 session、worktree 或 Android Outbox 状态。

Dashboard 的 Rust mobile-relay feature 负责保存 Relay center 与 connector 配置、启动或停止 `tw serve` / `tw relay-host`，也可把 bundled CLI 安装到用户明确选择的稳定 SSH 主机上启动 broker。选择写入 `mobileRelay.brokerHostId`，与开发 session 运行在哪个 Host 无关。Dashboard 托管的 broker 只监听远端 loopback；一键设置优先复用该 center 已保存的固定 WSS，否则通过远端已有的 `cloudflared` 建立临时 Quick Tunnel，读取并校验生成的根 `wss://` URL。Quick Tunnel URL 发布后，Node connector 负责重试 DNS 传播；若 macOS `getaddrinfo` 仍命中旧的负缓存而公网 A/AAAA 已可用，只对该 Quick Tunnel 使用直接解析结果建立连接，原 WSS hostname 与 TLS 校验保持不变。Relay center 不需要解析自己的公网 URL，阻塞的 SSH 编排运行在 Tauri 后台任务中，不占用 UI 线程。它不会创建 Mac LAN forward、公开明文 broker 或合成 `.local` URL。connector 的 Node.js 实现仍位于根 `src/`。

broker、WSS ingress 与 connector 在后端仍是可诊断的独立生命周期，但 Dashboard 的 **Set up Relay** 会按顺序编排它们：取得可信 WSS、部署/重启远端 broker 并轮换 Relay v1 shared secret、原子保存配置，再启动 Mac connector。固定 WSS 和单独 Save/Start 仍作为高级运维与恢复入口；`Stop connector` 只管理 Mac connector 与其本地 loopback `tw serve`。Dashboard 的 `connected` 只证明 Mac connector 已连接 broker，不证明 Android 在线；broker 部署成功也只是一轮命令结果，不伪装成持续健康状态。

Dashboard 的 Relay v2 self-hosted 路径是与上述 v1 owner 分离的显式 deployment port。renderer 只提交非敏感配置与本机 TLS 文件路径；bind address默认空且只接受显式输入的RFC1918 IPv4，`0.0.0.0`仅作为用户明确选择的全网卡例外。Tauri 串行拥有配置持久化、既有 SSH transport、完整 canonical `tw-cli/` directory staging、明确限定的 Linux x86_64 / Node.js >=22.16 / ext-family filesystem versioned bundle 发布、远端 tmux Center lifecycle 与状态探测；远端 preflight 在创建 self-hosted layout 或启动 Center 前拒绝旧 Node，且不改变通用 CLI/Dashboard 的 Node.js 20+ 要求。远端 root/tls/state/bundles/bootstrap 的每个既存对象都必须通过 no-symlink、real directory/file、current remote uid、exact mode 检查；scp staging 再递归拒绝 symlink/special/hard-link/foreign-owner，之后 dirs=0700、files=0600收敛并复核关键文件。`current` 只由 `ln -s` 后的 GNU `mv -Tf` 原子替换，避免跟随已有 directory symlink。TLS key、leaf certificate、独立 CA 先由 Tauri 以 `O_NOFOLLOW|O_CLOEXEC` fd打开，在lstat/fstat/read/fstat identity保持一致后只把descriptor bytes经SSH stdin发布；deployment profile固定0600，SQLite credential/continuity/keyring directory固定0700。CLI 参数只由单一 builder 组装：`--v2-single-node-self-hosted` 配合 `--host`、`--port`、独立 HTTPS advertised origin、TLS key/cert 和 state-directory path；没有 v1 shared secret 或 credential value。保存后的参数 fingerprint 必须与远端 profile 一致才允许启动。

Center start 对 live tmux session 是幂等 no-op，stop/restart 保留 state directory，并且正常 restart 省略 `--host-bootstrap-output`。首次尚无 Host bootstrap correlation 时，owner 必须创建并持久化一个新的非敏感 attempt identifier和一个远端 output path，并在同一次 Center invocation 同时传入 `--host-bootstrap-output <same-path>` 与 `--v2-self-hosted-bootstrap-correlation <same-attempt-id>`；若该首次启动在产出或ack前失败，repair只复用这对pending correlation/path，绝不创建第二个filename，credential是否仍需首次bootstrap继续由Broker durable publication authority决定。Tauri 通过复用同一 `~/.tmux-worktree/ssh/%C` ControlMaster 的非交互 SSH stdout cut读取该 owner-only 0600 file，限制为一个最多 8193-byte 的 `twhostboot2` record，原子写入本机 private 0600 file，确认后删除远端副本；renderer 永远只获得 availability。若启动时已有 self-hosted config，deployment owner先检查/创建本机 `~/.tmux-worktree` 与native contract固定的 `relay-v2-host-credential-atomic-file-cell-v1` exact 0700 directory，并在自身private 0700 namespace准备0600 CA copy、严格Host production profile input及存在时的bootstrap input；native fixed directory path不进入Node argv/options/authority。deployment owner 向 Host 分支只提供 issuer/WSS root、本机profile/CA path 与可选 bootstrap input path的窄 handoff，不解析 token、不签发 credential，也不复制 Host、worktree 或 tmux 业务。Start Center 在 exact self-hosted management child ready并提交本地input消费后，通过同一 `MobileRelayV2ManagementCommandState` 发送唯一 protocol-v2 `start_connector`；该操作复用canonical controller自身等待`host.registered`的完成边界，Tauri只在严格projection同时证明六项base capability时返回成功。失败不停止可能早已存在的Center、不remint credential/bootstrap，也不fallback；connector attempt清理由canonical controller持有，incomplete registered cut保持enrollment不可用并作为失败返回。因此这条一键编排只建立Host注册前置条件，不能解释为Android在线、production readiness或整体GO。

self-hosted Host activation继续使用真实Mac account home、真实本地TW managed state/session，以及canonical默认 `terminal-control-v1.sock`/state；它只在既有configLoader seam注入完整静态 `{hosts: []}`，因此不读取、筛选或补全account config中的remote Host。当前对外Scope严格只有Mac本地scope，Linux devbox只承担Broker Center。trusted production不注入该loader，继续严格解析完整account Host config；`--local-development`仍使用隔离trusted home、socket与state。

Relay v1 支持 Host/Scope/Session snapshot、创建、关闭、发送 agent message 以及 terminal input/output，但没有：

- Agent 入站历史或完整双向 Timeline；
- Waiting/Failed/Completed 等 Agent lifecycle event；
- 幂等 command ledger 与结果查询；
- 带 offset 的 terminal replay/resume；
- role-scoped credential 和 enrollment runtime。

因此 Android Session detail 的 Timeline 只把本机发起的消息及投递状态作为持久事实，不能伪造 Agent 回复或从终端输出推断 Agent lifecycle。Relay v1 shared secret 和终端内容只受 TLS 保护，生产 Android 配对必须使用可信 `wss://`。同一把 v1 secret 也不能为无关用户提供租户隔离；面向多用户的统一 Relay center 必须等待独立的 Relay v2 role-scoped credential 与 enrollment 实现。

### 共享 terminal input 与 Feishu Bridge

Dashboard、受控 `tw attach`、`tw serve`、Relay v1 和 Feishu Bridge 的产品级真实写路径都映射到同一个 `controlTargetId`，并由目标主机上的 terminal-control authority 在一次 critical section 内完成 lease/fence 校验和 backend write。managed single-pane 在该 authority 边界只使用逻辑 pane `"0"`；tmux 受 `pane-base-index` 影响的物理 pane index 只属于 attach transport。Relay terminal stream、Android 重连和 Feishu awaiting turn 都不是 input lease；observer 可以继续读 output，但不能据此取得或恢复写权。Dashboard、APK/Relay、受控 CLI 和 `tw serve` 在首个真实 input 时加入同一个 interactive lease/fence，多个实例可并行连接并由 single writer 串行提交 operation；某个 attachment release 只丢弃自己的本地 lease，不 fence 其他 interactive writer，最后一个登记的 producer 正常 release 时回到 `FREE`。只有 Feishu acquire/handoff 会把 interactive 端变为只读。relay-host 会续租活跃的 interactive lease，并在提交新输入前丢弃过期缓存、重新加入当前 shared lease；这个准备过程发生在 input operation 之前，因此不会重放可能已经执行的输入。Android 的 resize 只调整该 Relay attachment 的私有 PTY，不进入 terminal-control `input.resize`，也不改变 Dashboard 共享的 tmux 几何尺寸。异常退出后无人续租的 interactive lease 仍会在 exact target 复核和 output capture 换代后安全回到 `FREE`，Feishu 与不确定副作用继续 fail closed。Relay v1 无 ownership observation，Android 只对格式完整的 authority marker 在当前 stream 上本地 fail closed；fresh stream 会清除旧 latch，但不会重放被拒绝或排队的输入，若 Feishu 仍持有，下一次新的用户输入会再次被 authority 拒绝。这个 latch 不能被描述为 APK 持有服务端锁。

self-hosted不建立第二个TerminalControlAuthority，也不使用独立socket/state；它必须消费上述canonical daemon及该daemon同时拥有的 `.relay-v2-exact-*` sibling ingress。若默认base socket上的旧Dashboard CLI daemon仍存活但未提供sibling，Host preflight明确fail closed，hidden child不得启动、替换或关闭incumbent。首次迁移必须由用户显式升级并重启Dashboard terminal-control owner；新bundled daemon在同一authority/controlEpoch/lease/fence下同时服务base与exact ingress。

terminal-control 的 output capture 只是 Feishu marker correlation 的旁路事实，不是 tmux scrollback，也不是 Dashboard、`tw serve`、Relay 或 Android 实际展示的 PTY output。每个 `outputGeneration` 使用两个各 4 MiB 的 segment，并以 generation 内单调递增的绝对 byte cursor 对外；滚动后通常保留最近约 4–8 MiB。落在已淘汰范围内的 `output.tail` 返回 `STALE_OUTPUT_CURSOR`，但 authority 不因此把 target 置为 `RECOVERY_REQUIRED` 或锁住 interactive input。Bridge 只有在重新复核同一 Feishu lease、fence、control epoch 和 output generation 后，才可从保证保留的 4 MiB 起点重建只读 marker parser；它不重放 terminal input，任何 authority 变化仍 fail closed。raw `pipe-pane` 字节只用于确认本轮 nonce 的闭合 marker 已经到达，不能直接成为群卡片正文：Codex 等全屏 TUI 会在 marker 之间重绘输入框和 footer。闭合后 Bridge 必须使用 exact Feishu lease、pane、fence、control epoch 和 output generation 请求 terminal-control 的有界 rendered snapshot，并只从 tmux 已应用光标移动和擦行语义后的快照提取正文；快照缺少完整 nonce 时继续等待或最终 fail closed，绝不回退到 raw ANSI stripping。terminal-control 通过 `ping.capabilities` 增量声明 `output.rendered-snapshot`；Bridge 在取得或恢复 Feishu ownership 前预检，并在每个已授权群事件创建 turn、记 dedup 或注入 input 前再次检查。旧 controller 不带该字段等价于不支持，必须先升级，不能在已注入 prompt 后才发现无法安全提取。generation fence 完成后旧 generation 文件由 controller 回收。升级前已经因旧单文件容量上限进入 recovery 的 target，可以按既有 idle non-Feishu observation-repair 规则换代 capture：exact backend 必须可复核、ownership 为 `FREE`、没有 in-flight/operation/handoff uncertainty，且 previous owner 不能是 Feishu。backend identity、Feishu turn、handoff 或 operation disposition 不明仍然 fail closed。

Feishu Bridge 是独立本地 daemon，拥有群 binding、群成员精确 @Bot 触发策略、event dedup、单轮 marker/output cursor、幂等回帖和 binding 生命周期群通知。Dashboard 的正常绑定不要求管理员 Open ID：同一群内任意真实用户均可 @Bot 发起一轮，Bot/self、非用户事件和未 @Bot 的普通消息不会注入 session。每个 target 仍只有一个 marker-correlated reply turn；该 turn 活跃时，后续授权消息以独立确定性 operationId 进入同一个 terminal-control single writer，作为 steering update 复用原 turn 的 marker/output correlation，而不是创建竞争 turn。最终回复仍锚定第一条源消息，steering 消息只尝试发送幂等的 best-effort 状态确认；任一 steering 写入 disposition 不确定都会使原 turn 和 binding fail closed，不能重放。Claude/Codex 的 inherited structured-result watch 在没有群输入时继续发送自动完成卡；首条群消息只有在 canonical input 被确认接受后才取消该 watch 并把任务转换成普通 Feishu steering turn。Kimi 没有 structured inherited-result source，但同一 start-or-steer prompt 可直接进入已运行的 Kimi。绑定完成、手动解绑或 exact target 确定消失/被同名生命周期替换后，Bridge 用 bot 身份发送顶层 Card JSON 2.0 状态卡；Dashboard 和 CLI 发起的解绑会在卡片中记录真实本地入口，缺失来源时只描述为未知本地管理端，不伪造“用户主动”。Agent 的确定最终回复仍是源消息的 Card JSON 2.0 回复，但其落点由 binding 的 `replyMode=topic|direct` 决定：默认 `topic` 进入源消息话题，`direct` 留在群主消息流且不会在两种模式间 fallback。旧 binding 缺少该字段时按当前既有行为归一化为 `topic`。绑定时可选择落点，Settings 通过 Bridge 的 canonical mutation 修改；有 active turn 时修改被拒绝，避免同一轮中途改变回复位置。单轮 `deadlineAt` 是滑动的无输出期限，不是 Agent 总运行时长：同一 target/epoch/generation/fence 下每次观察到新字节或确认一个 steering operation 都会重新开始十分钟等待，轮询在宣告无输出前还会做一次最终 fenced read。Dashboard 的 pane-title `agent_running` 只属于展示启发式，不能授权 Bridge 发送；最终回复仍必须由本轮闭合 marker 和相关 rendered snapshot 确认。发送前，Bridge 在一次原子存储提交中记录已经过 terminal authority 证明、清洗并限长的完整卡片载荷、落点、最终 turn 状态和确定性幂等键；daemon 重启会用同一键续投仍为 `prepared` 的载荷，旧版缺少载荷的 prepared 记录则转为 uncertain。回复或 inherited completion 无法确认时不盲发 Agent 内容，而是单独排队一张不含 Agent 输出的恢复状态卡。Bridge 在启动和维护周期检查全部 active/paused/stale binding 的精确 `controlTargetId`，只对确定结束的生命周期删除 binding；controller 不可用、recovery 状态或单纯 authority lineage 丢失不会被伪报成删除/替换。同名新 session 永不自动继承旧 binding。事件消费进程的 `starting/running/backoff` 状态由 Bridge 暴露；初次 spawn 失败会阻止 daemon 伪装 ready，运行中退出会进入重连，Dashboard 在不可用时禁用新绑定并显示状态。生命周期卡片与 `Typing`/`CrossMark` reaction 都在独立 best-effort 展示队列中，不参与 binding、reply disposition 或 terminal authority；发送延迟/失败不能阻塞 lease renewal 或回滚已提交的 mutation。Dashboard 可以按需启动和管理 Bridge，但退出 Dashboard 不得停止共享 Bridge/controller 或隐式释放 active Feishu ownership；它只丢弃自己的 interactive lease view。Tauri 通过 `bridge.info` 的 profile 与 capability 做 rolling-upgrade 判定：空 legacy daemon 可安全替换；已有 binding、缺少 structured result、steering、consumer health 或 durable reply capability 的 daemon 仍可查询、canonical remove 和 controlled takeover，但不能 create/resume/repair/return 到会接受群输入的 ownership；可选回复模式 capability 仍只约束 Direct 创建或修改。更旧的 occupied daemon 继续只允许既有检查/解绑路径；stale/pausing legacy binding 只有在它是唯一 binding 且无 active turn 时才迁移。Feishu 占用时 Dashboard 的本地 takeover/pause 操作负责 drain 或取消当前 turn，再原子切回 interactive 类。Settings 只持久化非敏感的 `lark-cli` profile 名称，bot credential 继续由 `lark-cli` 拥有；其 Group bindings 列表通过 Bridge 的 canonical remove 入口解绑，无 live lease 时只有 authority 已证明 Feishu 不再持有才可移除；Terminal 和 Settings 两个入口都必须显式确认。更换 profile 只允许在没有任何 binding/turn 的空 Bridge 上执行。完整状态机、handoff commit point、Relay v1/v2 拒绝规则和 privileged raw-tmux bypass 见 [`docs/terminal-input-ownership-alignment.md`](docs/terminal-input-ownership-alignment.md)。

## 状态所有权

### Mac / CLI / remote Host

| 状态 | 权威 owner | 允许的主要写入者 | 说明 |
|---|---|---|---|
| live tmux sessions/panes | 各目标主机的 tmux server | `tw`；明确的 legacy compatibility path | 表示进程是否真实存在，不表示是否 TW-managed |
| git repositories/worktrees | 各目标主机的 git | `tw` managed create；Dashboard orphan cleanup | 工作区内容和 dirty 状态由 git 决定 |
| `~/.tmux-worktree/state.json` | 目标主机上的 `tw` | `tw` session/RPC lifecycle | managed worktree/terminal registry；mutation 使用跨进程锁和原子替换，损坏或未知 schema 时 fail closed |
| `~/.tmux-worktree.json` | 用户配置 | CLI 和 Dashboard | projects、hosts、worktree base、mobile relay，以及非敏感的 `feishuBridge.larkProfile`；跨进程写入必须持锁并保留未知字段，Lark credential 不写入这里 |
| `~/.tmux-worktree/relay-v2-self-hosted/dashboard-config-v1.json` | Dashboard Relay v2 deployment owner | 显式 self-hosted opt-in、SSH Host、用户输入的 HTTPS origin、bind host/port、本机 TLS key/leaf/CA path 与非敏感 bootstrap filename；不含 token/credential |
| `~/.tmux-worktree/relay-v2-self-hosted/private/host-bootstrap-*.twhostboot2` | Dashboard Tauri → canonical Host trusted source | 首次 Host 的有界 owner-only 0600 bootstrap input；不进入 renderer，Host credential 已持久化后的 restart 不再请求新 token |
| `~/.tmux-worktree/relay-v2-self-hosted/private/{host-production-profile-input-v1.json,host-tls-ca-input.pem}` | Dashboard deployment owner → canonical Host trusted source | fd-bound选中CA的0600私有copy与冻结0600非敏感Host profile input；不含native目录path |
| `~/.tmux-worktree/relay-v2-host-credential-atomic-file-cell-v1/` | Host native trusted factory fixed namespace | Dashboard只在self-hosted child前创建/校验current-user-owned exact0700目录；Node不接收path，native factory仍按account database home与contract固定components自行打开 |
| remote `~/.tmux-worktree/relay-v2-self-hosted/` | Dashboard deployment owner / canonical Broker CLI | versioned完整 `tw-cli` bundle、0600 TLS/profile、0700 bootstrap staging 与 dedicated SQLite state；stop/update 不删除 credential/continuity/keyring state |
| `~/.tmux-worktree/relay-v2-host/profile-v1.json` | Relay v2 Host production profile store | `loadOrCreateRelayV2HostProductionProfile` | reference-only、create-once 的固定 contract；operator 只能经显式 v2-only 0600 input 导入，exact existing 幂等，different/corrupt/unsafe 不覆盖；不含 credential/bootstrap/refresh secret |
| `~/.tw-dashboard-terminals.json` | Dashboard metadata | Dashboard Rust 与 relay-host | label、cwd、host/raw name、managed marker；不是 tmux 或 managed state 的替代品 |
| `~/.tw-dashboard-layout.json` | Dashboard | Dashboard Rust | canonical schema、revision/CAS、窗口与工作区展示状态，包括 worktree/terminal 侧栏顺序 |
| `~/.tw-dashboard-automations.json` | Dashboard/CLI 共用定义 | Dashboard Rust 与 `tw automation` | 本机 automation 定义；调度器随 Dashboard 进程运行 |
| `~/.tw-dashboard-automation-runs.json` | Dashboard | Dashboard Rust | 本地、有界的运行历史 |
| `~/.tw-dashboard-pending-worktree-cleanup.json` | Dashboard | Dashboard Rust | session 关闭后尚未完成的本机 worktree 清理队列 |
| `~/.tw-serve-token` | `tw serve` | `tw serve` | 本地 Web terminal bearer token |
| `~/.tmux-worktree/mobile-relay-status.json` | connector runtime | 当前 relay-host instance | 可丢弃的连接状态，不是配置或 session 权威 |
| `~/.tmux-worktree/terminal-control-state-v1.json` | local terminal-control | terminal-control daemon | exact target、control epoch、Feishu/interactive ownership、共享 lease/fence、handoff 与 operation disposition；严格 schema、锁和 0600 原子写 |
| `~/.tmux-worktree/terminal-control-output-v1/` | local terminal-control | terminal-control daemon | Feishu marker correlation 使用的有界、generation-fenced 两段式 capture；不是可见 PTY/scrollback，不授予 input ownership；旧 generation 在 fence 后回收 |
| `~/.tmux-worktree/feishu-*.json` | Feishu Bridge | Feishu bridge daemon | 私有 binding、event dedup、turn 和 outbound reply disposition；不是 lease authority |

`profile=cli|dashboard` 只是 managed record 的来源标记，不选择不同的 tmux 布局。新 managed worktree 和 terminal 都是 single-pane contract：可选的 AI command 在唯一 pane 中运行，退出后回到 login shell。Dashboard 的 worktree 创建只传入所选目标上已探测可用的固定 Agent 命令，Dashboard 的 terminal 创建始终省略命令并直接进入 login shell；CLI/RPC 仍保留显式可选命令能力。

### Android

| 状态 | Owner | 说明 |
|---|---|---|
| `tw_mobile_v2.db` | Android repository/Room | 当前 pairing 下的 Host、Scope、Session cache、Timeline、Outbox 和 checkpoint；profile 切换时按 barrier 清空 |
| `tw_mobile_relay_v2_state.db` | Android Relay v2 state repository | 已验证显式 v2 profile 的 base-sync authority/Scope/opaque Session namespace、pinned snapshot staging 与 durable event buffer；Outbox query/recovered、bounded fresh durable dispatch、受限 Session reply enqueue、已 admission 的 existing-Session kill，以及每个 materialized 可见 Scope（包括空 Scope）的 durable `create_worktree` / `create_terminal` 已接 base composition；额外 extension command UI 与 Agent runtime仍未接，且不含 v1 行或 credential |
| `tw_mobile_v2_preferences` | Android DataStore | Relay URL、首选 Host/Scope、自动连接和通知偏好；不保存明文 secret |
| Android Keystore + secure preferences | credential store | Keystore 保存不可导出的 key，SharedPreferences 只保存 AES-GCM IV/ciphertext |
| actor 内存状态 | `RelayV1ConnectionActor` / `RelayV2BaseRuntimeComposition` | v1 持有 socket epoch、pending request、stream generation 与重连；受限 v2 composition 独占 exact profile 的 actor/effect/close lifecycle，并只对 exact retryable failure 以单一有界 backoff owner 重连；每个 successor 复用同一 actor 且重读 Room Outbox。Compose 组件不能直接修改 |

Android cache 不是远端权威。离线时可以继续展示 stale snapshot 和 Outbox，但不能把本地缺失解释为远端资源已删除。

## Managed lifecycle 与兼容层

新代码必须维持以下边界：

1. **Canonical-first mutation。** create/restore/kill 先走目标主机的 TW RPC。UI 传来的 `managed` 是缓存提示，不能覆盖真实 managed state。
2. **写入 fail closed。** state、terminal registry、layout 或 config 无法安全解析时，不得用空对象覆盖原文件。共享文件写入使用相应跨进程锁与原子替换。
3. **兼容发现不等于兼容创建。** catalog 可以发现符合严格路径、git 和 tmux shape 的旧 session；远端 mutation 不能因此退回旧 creator。
4. **Legacy kill 范围有限。** canonical kill 只有明确返回“不是 TW-managed”、旧 RPC 不支持或远端没有 `tw` 等兼容信号时，才允许 direct tmux kill；state corruption、timeout 和普通执行错误不能授权 fallback。
5. **Terminal metadata 不复活 managed runtime。** 带 `managed=true` 的持久 terminal 只检查 live 状态；已消失就移除 metadata。缺少 marker 的旧记录才可以使用 `ensure_terminal_session` direct-tmux 恢复路径。
6. **配置格式继续兼容。** projects 接受历史对象/数组和 `projects`/`repositories`/`repos`，路径、分支和 worktree base 也保留已支持的字段别名；新写入不得无故清除未知字段。
7. **历史 worktree 只用于发现。** 默认新根目录是 `~/.tmux-worktree/worktrees`；`/private/tmp/tmux-worktree/projects` 仅作为 legacy discovery 边界。
8. **Android 升级 shim 有明确范围。** `MainActivity` 只重定向到 `V2Activity`；`LegacyIdentityImporter` 迁移旧明文 identity 到 DataStore/Keystore 后清理 secret。APK 内 xterm/WebView 是当前生产实现，不是迁移层。
9. **Relay v1 兼容保持局部。** Android 只在旧响应完全省略 `requestId` 时，按当前 request registry 做受限关联；未知非空 ID 必须视为迟到响应。Relay v2 必须另建 actor/codec/persistence，不能把 v2 语义散落进 v1 实现。
10. **Terminal input fail closed。** 所有受支持 writer 必须通过 target-scoped terminal-control；authority 不可用、lease/fence 不匹配、handoff pending 或 continuity 不明时不得回退 direct tmux/PTY write。Relay v1 wire 保持冻结，只用现有 error 拒绝；Feishu transport 与 Relay credential 永远分离。

兼容层的删除条件必须同时包含：不再支持对应升级来源、迁移遥测或明确版本策略证明安全，以及相关 contract/test 已同步更新。不能仅因当前开发机没有旧数据就删除。

## 构建与发布边界

### 根 CLI

根 `npm run build` 运行两套 tsup 配置：一套生成可被测试/import 的 ESM 模块，另一套把 `src/cli.ts` 及依赖打成唯一 standalone Node entry `dist/cli.cjs`。不要把 `dist/*.js` 当作可复制的 CLI。

Broker credential N-API 的 build/stage/npm-pack verification 是独立 opt-in foundation：普通 build 只生成共享 fixed target descriptor与loader JavaScript，不运行 Cargo、不stage `.node`、也不触发 pack verifier。显式 stage 必须紧跟一次 clean-dist普通build，并只从descriptor对应的exact Cargo target artifact建立loader相邻fixed `.node`；所有header/digest/空目录证明都在hard-link唯一commit point前完成，commit后final永不rollback，只identity-check清理脚本自建0700随机temp。stage 与 pack layout 按 owner 精确校验本 owner artifact：共享 `dist/relay/v2/native` 中只允许同 target 的冻结 Host credential sibling artifact共存（由其 fixed descriptor 模块枚举、不校验内容），未知/重复/错 target 或非 regular file 一律拒绝；pre-existing 目录只在只含允许 sibling 时被 adopt，绝不替换或清理。显式 pack verifier只处理自己用`npm pack --ignore-scripts`生成的临时tarball/解包目录，并以有界typed ustar parser拒绝link/special/PAX/GNU longname，只流式写出allowlist内的regular file。该Node foundation持有parent fd并检查dev+ino，但Node path API没有`openat/linkat/unlinkat`，所以不宣称抵抗恶意同uid在最终目录上并发换名；观察到identity mismatch时保留对象并显式失败。它不通过`prepack`/`prepare`/`postinstall`接入npm lifecycle，也不改变Relay v1/CLI/Dashboard runtime。

Host credential N-API artifact 有一条同构但完全独立命名空间的 opt-in foundation：普通 build 只生成 Host 的 fixed target descriptor 与 fixed loader JavaScript，不运行 Cargo、不 stage `.node`、也不触发 pack verifier。显式 `build:relay-v2-host-credential-native` 同样要求紧跟一次 clean-dist 普通 build，只从 descriptor 对应的 exact Cargo target artifact（`native/relay-v2-host-credential-atomic-file-cell-napi` cdylib）经同一 fd-owned stage 边界建立 loader 相邻的 Host fixed `.node`；header/digest/空目录证明同样在 hard-link 唯一 commit point 前完成，commit 后 final 永不 rollback；stage 与 pack layout 同样按 owner 精确校验本 owner artifact，只允许同 target 的冻结 Broker sibling artifact 共存。显式 `verify:relay-v2-host-credential-native-pack` 复用同一 bounded ustar inspector，校验 unpacked package 元数据与上述 layout，并从 unpacked 包导入 fixed loader/holder 验证 revision 7 raw artifact 保持 exact v1 单 `open` 表面、trusted factory entry 存在于 raw open function 上、raw open 在空 qualification allowlist 前 `CELL_DURABILITY_UNSUPPORTED`、trusted factory 的两条 contract-closed 分支（无部署目录的 error union 或目录合法时 ready→one-shot bind→final exact-open→gate）均闭合且 replay 固定 `CELL_CLOSED`，再跑 env-gated binding 测试。它同样不接入 npm lifecycle、不改变 Relay v1/CLI/Dashboard runtime，也继承 Node path API 无 `openat/linkat/unlinkat` 的同一声明；artifact 存在或可加载不产生 qualification/readiness/capability，当前只有本机 Darwin arm64 证据。

根 npm package 发布 `dist/`、Dashboard installer 和准备好的 DMG 目录；它不构建 Android。

### macOS Dashboard

Tauri `beforeBuildCommand` 先构建 React，再构建根 CLI；`tauri.conf.json` 把完整 `dist/` sibling tree 和根 `package.json` 放入 `.app/Contents/Resources/tw-cli/`。这形成源码级分层、产物级组合：Dashboard 自己不实现 managed creator，但随包携带精确版本且保持 ESM/import identity 的 canonical control plane。

`app/scripts/release.sh` 当前负责 macOS bundle/DMG 构建、签名完整性校验和把 DMG 复制到 installer 目录；上传、notarization 和发布渠道在脚本外处理。脚本不发布 APK，也不把 Node.js 嵌入 `.app`。

### Android

Android 使用自己的 Gradle wrapper、依赖图、测试和 Lint。根 npm build、Tauri build 和 macOS release script 都不会编译它。当前 Gradle 工程没有 production signing config，因此源码中的 `assembleRelease` 只产生未签名构建验证产物；可分发 APK 的签名和发布必须由外部 release 流程显式完成。

一次统一版本发布需要同步根 `package.json`、`app/package.json`、Tauri config、Rust package 和 Android `versionName`/`versionCode`，并分别验证 CLI、Dashboard/Rust 和 Android。不要用硬编码的测试数量判断完成；执行各工程当前定义的完整门禁。

## Contract 与文档分类

| 位置 | 类型 | 维护规则 |
|---|---|---|
| `README.md` | 用户与开发入口 | 安装、Quick Start、配置、构建和 release 命令；不承载深层实现细节 |
| `AGENTS.md` | Agent 入口 | 阅读顺序、架构红线、变更到验证的矩阵和交付规则 |
| `ARCHITECTURE.md` | 当前实现 | 代码地图、数据流、状态 owner、兼容与发布边界；只能描述已经落地的事实 |
| `docs/android-v2-architecture.md` | 当前 Android 专题 | Compose/Room/actor/Outbox 的详细产品和实现约束 |
| `docs/remote-relay-android.md` | 运维手册 | broker、Mac connector、TLS、配对和 Android 验证流程 |
| `docs/terminal-input-ownership-alignment.md` | 跨产品实现 companion | terminal-control、Feishu、Dashboard、CLI 与 Relay 的 lease、handoff、拒绝行为和测试矩阵 |
| `docs/feishu-meeting-agent-integration-plan.md` | 未实现的集成提案 | Feishu Bot 真实入会、会议事件、Agent routing、恢复和分阶段交付；不能据此宣告当前已支持入会 |
| `docs/relay-v2-contract.md` | 冻结契约 | codec conformance 与未接线的 H0/H1/H2 spool/H3、Android HTTPS foundations，以及已接受限 base-sync production root 的 Android actor/Room/bounded RFC6455 WSS 已落地；另有 revision 2 native broker credential store contract（interface/storage/binary/fixture仍v1）、未接线纯 Rust core/platform-common lifecycle owner、Darwin/Linux adapter、raw N-API binding、optional loader和credential authority source foundations，但空 durability allowlist不允许platform-adapter-qualified real open；显式未接线的本机 target build/stage/npm-pack verifier已有Darwin arm64 fixed-layout/closed-binding证据，但没有其他target、matrix、bit reproducibility、Dashboard/production shipping、qualified open、production injection/wiring或ready；Android WSS 已接受限 base-sync composition，但无真实 TLS/device 互操作证据；跨端完整 runtime/capability 仍未交付，不能据此宣告 capability 或生成 v2 credential |
| `docs/relay-v2-implementation-plan.md` | 非规范性实施协调 | 按 owner 拆分可并行工作、硬依赖和验收 Gate；不描述当前完成状态，也不修改冻结契约 |
| `contracts/` | 机器契约 | Relay v1/v2、TW RPC v1 和共享 storage fixture；协议/存储变更必须同步生产实现和 contract tests |
| `.codex/skills/` | Agent 操作规程 | 固化跨机器等易误操作流程，不属于产品 runtime |

历史重构计划、一次性 QA 记录、临时截图路径和已经完成的迁移清单不应伪装成当前架构基线；需要追溯时使用 Git 历史。新增文档前先判断它属于用户手册、当前架构、专题运维、未来 contract 还是 Agent 规程，避免复制同一事实到多个位置。

## Relay v2 host codec/H1/H2/H3 composition foundation

The default-off Relay v2 host composition owns one codec readiness producer
statically bound to the production strict codec. It publishes only fixed
generation `1` with literal synchronous readiness, while base and carrier
composition expose only its frozen `close()` lifecycle. The same composition
closes the isolated H1, recovered-H2, and H3 authority chains. H1 accepts only
the opaque one-shot candidate signed
inside the command plane's final recovered HostState audit cut. H2 accepts only
the canonical snapshot spool's one-shot candidate for an exact recovered pinned
cut and completes its private receipt/lease/readiness activation before async
composition open returns; when no recovered pinned cut exists, the spool's
one-shot fresh-install bootstrap candidate feeds the same private activation
pair instead. H3 accepts only the durable lineage/manager/host
recovery candidate. Runtime receives private gated facades; public H1 and H2
each expose only `close()`, while H3 has no readiness `apply` path. Close,
disposal, owner/cut withdrawal, or fatal authority failure synchronously
withdraws readiness and applies the existing 4406 route fence. This foundation
remains outside `relay-host` CLI/public production activation; the actor's static empty
advertised capability set is only the fail-closed input that forbids bypassing
the readiness-to-pre-carrier-claim chain, and the codec producer alone does not
advertise Relay v2 capabilities.
